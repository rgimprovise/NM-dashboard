# ⏱️ ЧАСТОТА ОБНОВЛЕНИЯ ДАННЫХ В ДЭШБОРДЕ

**Дата:** 15 ноября 2025  
**Версия:** vortex-home 2

---

## 📊 ТЕКУЩАЯ АРХИТЕКТУРА ОБНОВЛЕНИЯ ДАННЫХ

### 🔄 Когда обновляются данные:

#### 1. **При первой загрузке страницы**
   - При открытии Dashboard, Marketing или Sales
   - React `useEffect` выполняет запросы к API
   - Загружаются данные за выбранный период (по умолчанию 30 дней)

#### 2. **При изменении периода**
   - Пользователь выбирает новый период: 7д / 30д / 90д
   - `useEffect` с зависимостью `[period]` запускает новую загрузку
   - Данные перезапрашиваются с новыми датами

#### 3. **При переключении между страницами**
   - Каждая страница (Dashboard, Marketing, Sales) имеет свой `useEffect`
   - При переходе на страницу данные загружаются заново
   - Нет глобального кэша между страницами

#### 4. **При ручном обновлении браузера**
   - F5 / Cmd+R - полная перезагрузка страницы
   - Все данные загружаются с нуля

---

## ❌ ЧТО НЕ РЕАЛИЗОВАНО:

### Нет автоматического обновления по таймеру

**Код компонентов НЕ содержит:**
```typescript
// ❌ Такого кода НЕТ в проекте
setInterval(() => {
  loadData();
}, 30000); // Обновление каждые 30 секунд
```

**В результате:**
- Данные НЕ обновляются автоматически каждые X секунд
- Пользователь видит "статичные" данные до изменения периода или обновления страницы

---

## 🏗️ АРХИТЕКТУРА ЗАПРОСОВ

### Frontend (React)

#### Dashboard.tsx
```typescript
useEffect(() => {
  const loadData = async () => {
    // Загрузка данных при монтировании и изменении period
    const [summaryData, productsData, ordersData] = await Promise.all([
      dataService.getDashboardSummary(period),
      dataService.getTopProducts(period),
      dataService.getOrders(period),
    ]);
  };
  loadData();
}, [period]); // ✅ Триггер: изменение period
```

#### Marketing.tsx
```typescript
useEffect(() => {
  const loadCampaigns = async () => {
    const data = await dataService.getCampaigns(period);
    setCampaigns(data);
  };
  loadCampaigns();
}, [period]); // ✅ Триггер: изменение period
```

#### Sales.tsx
```typescript
useEffect(() => {
  const loadData = async () => {
    const [ordersData, productsData] = await Promise.all([
      dataService.getOrders(period),
      dataService.getTopProducts(period),
    ]);
  };
  loadData();
}, [period]); // ✅ Триггер: изменение period
```

---

### Data Service (dataService.ts)

**Кэширование:** ❌ НЕТ

Каждый вызов метода делает новый HTTP запрос к API:

```typescript
async getDashboardSummary(period: string) {
  // ❌ Нет проверки кэша
  // ❌ Нет TTL (Time To Live)
  // ✅ Прямой запрос к API каждый раз
  const [orders, campaigns] = await Promise.all([
    yandexAPI.getOrders(periodParams.from, periodParams.to),
    vkAPI.getCampaigns(),
  ]);
  return processedData;
}
```

---

### Backend API Routes

#### Yandex API (`server/routes/yandex.ts`)
- **getOrders**: Запрос к Yandex Market API
- **getProducts**: Запрос к Yandex Market API
- **getCampaigns**: Запрос к Yandex Market API

**Нет кэширования на сервере**

#### VK API (`server/routes/vk.ts`)
- **getCampaigns**: Запрос к VK Ads API
- **getStatistics**: Запрос к VK Ads API
- **getAdGroups**: Запрос к VK Ads API

**Нет кэширования на сервере**

---

## 🔐 АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ ТОКЕНА VK

**Единственная автоматизация:** VK Token Manager

### vkTokenManager.ts

```typescript
export async function ensureValidToken(): Promise<string | null> {
  // ✅ Проверка срока действия токена
  if (Date.now() >= TOKEN_EXPIRES_AT) {
    // ✅ Автоматическое обновление через refresh_token
    await refreshAccessToken(VK_REFRESH_TOKEN);
  }
  return VK_TOKEN;
}
```

**Работает:**
- ✅ Проверяет срок действия перед каждым запросом к VK API
- ✅ Автоматически обновляет токен через `refresh_token`
- ✅ Сохраняет новый токен в `.env`
- ✅ Сохраняет scope: `["read_ads", "read_payments"]`

---

## 📈 ТЕКУЩАЯ ЧАСТОТА ОБНОВЛЕНИЯ

| Сценарий | Частота | Примечание |
|----------|---------|------------|
| **Открытие Dashboard** | 1 раз при загрузке | useEffect mount |
| **Изменение периода** | Каждый раз | useEffect dependency |
| **Переключение страниц** | 1 раз на страницу | useEffect mount |
| **Автоматическое обновление** | ❌ НЕТ | Не реализовано |
| **Кэш данных** | ❌ НЕТ | Каждый раз новый запрос |
| **VK Token refresh** | ✅ Автоматически | При истечении срока |

---

## ⚡ ВОЗМОЖНЫЕ УЛУЧШЕНИЯ

### 1. Автоматическое обновление данных

**Добавить в компоненты:**
```typescript
useEffect(() => {
  loadData(); // Первая загрузка
  
  // Автообновление каждые 5 минут
  const interval = setInterval(() => {
    loadData();
  }, 5 * 60 * 1000);
  
  return () => clearInterval(interval);
}, [period]);
```

**Плюсы:**
- ✅ Данные всегда актуальны
- ✅ Пользователь видит изменения без обновления страницы

**Минусы:**
- ❌ Увеличенная нагрузка на API
- ❌ Возможно превышение лимитов API
- ❌ Повышенное потребление трафика

---

### 2. Кэширование на клиенте

**Пример с React Query:**
```typescript
const { data, isLoading } = useQuery({
  queryKey: ['dashboard', period],
  queryFn: () => dataService.getDashboardSummary(period),
  staleTime: 5 * 60 * 1000, // 5 минут
  cacheTime: 10 * 60 * 1000, // 10 минут
});
```

**Плюсы:**
- ✅ Мгновенное отображение закэшированных данных
- ✅ Меньше запросов к API
- ✅ Лучший UX при переключении между страницами

**Минусы:**
- ❌ Требует добавления библиотеки (React Query / SWR)
- ❌ Данные могут быть устаревшими

---

### 3. Кэширование на сервере

**Redis или Memory Cache:**
```typescript
// server/cache/redisCache.ts
export async function getCachedOrders(period: string) {
  const cacheKey = `orders:${period}`;
  const cached = await redis.get(cacheKey);
  
  if (cached) return JSON.parse(cached);
  
  const orders = await yandexAPI.getOrders(...);
  await redis.setex(cacheKey, 300, JSON.stringify(orders)); // 5 мин TTL
  
  return orders;
}
```

**Плюсы:**
- ✅ Снижение нагрузки на внешние API
- ✅ Быстрые ответы для всех пользователей
- ✅ Защита от rate limits

**Минусы:**
- ❌ Требует инфраструктуры (Redis)
- ❌ Сложность в инвалидации кэша
- ❌ Возможны устаревшие данные

---

### 4. WebSocket / Server-Sent Events

**Real-time обновления:**
```typescript
const eventSource = new EventSource('/api/dashboard/stream');

eventSource.onmessage = (event) => {
  const newData = JSON.parse(event.data);
  setSummary(newData);
};
```

**Плюсы:**
- ✅ Реальное время
- ✅ Нет polling
- ✅ Эффективное использование соединения

**Минусы:**
- ❌ Требует изменения архитектуры сервера
- ❌ Сложность в масштабировании
- ❌ Может быть избыточно для бизнес-данных

---

## 💡 РЕКОМЕНДАЦИИ

### Для текущей архитектуры:

1. **Короткий срок (0-2 недели):**
   - ✅ Оставить как есть
   - ✅ Добавить кнопку "Обновить" для ручного обновления
   - ✅ Показывать время последнего обновления

2. **Средний срок (2-4 недели):**
   - ✅ Добавить автообновление каждые 5-10 минут
   - ✅ Добавить индикатор "Обновление..." при загрузке

3. **Долгий срок (1-3 месяца):**
   - ✅ Внедрить React Query для кэширования
   - ✅ Добавить server-side кэш (Redis)
   - ✅ Оптимизировать количество запросов

---

## 📝 РЕЗЮМЕ

**Текущее состояние:**
- 🟡 Данные обновляются **только при действиях пользователя**
- 🟡 Нет автоматического обновления по таймеру
- 🟡 Нет кэширования данных
- 🟢 VK токен обновляется автоматически

**Для большинства бизнес-дэшбордов это приемлемо:**
- Данные обновляются не чаще раза в час в реальном бизнесе
- Пользователь может обновить страницу при необходимости
- Снижается нагрузка на API и избегаются rate limits

**Если нужны более частые обновления:**
- Следуйте рекомендациям выше по добавлению автообновления
- Учитывайте лимиты внешних API (Yandex, VK)

