# 🔄 АНАЛИЗ АРХИТЕКТУРЫ РОУТИНГА ДАННЫХ И РАСЧЕТОВ

**Дата:** 15 ноября 2025  
**Версия:** vortex-home 2  
**Статус:** Текущая архитектура БЕЗ кэширования

---

## 📊 ОБЩАЯ СХЕМА ПОТОКА ДАННЫХ

```
┌─────────────────────────────────────────────────────────────┐
│                      ВНЕШНИЕ API                             │
│  ┌──────────────────┐         ┌──────────────────┐          │
│  │  Yandex Market   │         │     VK Ads       │          │
│  │      API v2      │         │      API v2      │          │
│  └────────┬─────────┘         └────────┬─────────┘          │
└───────────┼────────────────────────────┼────────────────────┘
            │                             │
            ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Express)                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           server/routes/yandex.ts                    │   │
│  │  - getOrders(): Заказы (6 кампаний, пагинация)      │   │
│  │  - getProducts(): Товары (stocks)                    │   │
│  │  - getCampaigns(): Инфо о кампаниях                  │   │
│  │  - getOrderStats(): Статистика заказов               │   │
│  │  - getStocks(): Остатки                              │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │            server/routes/vk.ts                       │   │
│  │  - getCampaigns(): Кампании VK                       │   │
│  │  - getAdPlans(): Рекламные планы                     │   │
│  │  - getAdGroups(): Группы объявлений                  │   │
│  │  - getBanners(): Баннеры                             │   │
│  │  - getStatistics(): Статистика (ad_groups)           │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │      server/utils/vkTokenManager.ts                  │   │
│  │  - ensureValidToken(): Автообновление токена         │   │
│  │  - refreshAccessToken(): Refresh через API           │   │
│  │  - saveTokens(): Сохранение в .env                   │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────┬──────────────────────────────┬──────────────────┘
            │ /api/yandex/*                │ /api/vk/*
            ▼                              ▼
┌─────────────────────────────────────────────────────────────┐
│                FRONTEND API CLIENTS                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  client/services/api/yandexClient.ts                 │   │
│  │  class YandexMarketAPI {                             │   │
│  │    getCampaigns()                                    │   │
│  │    getOrders(dateFrom?, dateTo?)                     │   │
│  │    getProducts()                                     │   │
│  │    getOrderStats(dateFrom, dateTo)                   │   │
│  │  }                                                    │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  client/services/api/vkClient.ts                     │   │
│  │  class VKAdsAPI {                                    │   │
│  │    getAdPlans()                                      │   │
│  │    getCampaigns(adPlanId?)                           │   │
│  │    getStatistics(dateFrom, dateTo, campaignIds?)     │   │
│  │    getCampaignStats(campaignId, dateFrom, dateTo)    │   │
│  │  }                                                    │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────┬──────────────────────────────────────────────────┘
            ▼
┌─────────────────────────────────────────────────────────────┐
│                  DATA SERVICE LAYER                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │        client/services/dataService.ts                │   │
│  │  class DataService {                                 │   │
│  │    getDashboardSummary(period)                       │   │
│  │    getCampaigns(period)                              │   │
│  │    getOrders(period)                                 │   │
│  │    getTopProducts(period)                            │   │
│  │    getProducts()                                     │   │
│  │  }                                                    │   │
│  │                                                       │   │
│  │  🔴 НЕТ КЭШИРОВАНИЯ                                  │   │
│  │  🔴 Каждый вызов = новый HTTP запрос                 │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────┬──────────────────────────────────────────────────┘
            ▼
┌─────────────────────────────────────────────────────────────┐
│                    REACT COMPONENTS                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │ Dashboard   │ │ Marketing   │ │   Sales     │          │
│  │    .tsx     │ │    .tsx     │ │   .tsx      │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
│  ┌─────────────┐ ┌─────────────┐                          │
│  │ Products    │ │  Reports    │                          │
│  │    .tsx     │ │    .tsx     │                          │
│  └─────────────┘ └─────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ ПО СЕКЦИЯМ

### 1. DASHBOARD (Dashboard.tsx)

#### Источники данных:

```typescript
useEffect(() => {
  const [summaryData, productsData, ordersData] = await Promise.all([
    dataService.getDashboardSummary(period),
    dataService.getTopProducts(period),
    dataService.getOrders(period),
  ]);
}, [period]);
```

#### Что отображается:

| Секция UI | Источник данных | Расчеты | Статус |
|-----------|----------------|---------|--------|
| **KPI Cards (4 шт)** | `getDashboardSummary()` | | |
| • Общая выручка | `orders[].buyerTotal` | `sum(orders.buyerTotal)` | ✅ Реальные |
| • Всего заказов | `orders.length` | `count(orders)` | ✅ Реальные |
| • Расходы на рекламу | VK Statistics | Недоступны | ❌ 0 |
| • ROAS | `revenue / adSpend` | `totalRevenue / adSpend` | ❌ NaN (adSpend=0) |
| **KPI Cards Row 2** | | | |
| • Средний чек | `orders` | `totalRevenue / totalOrders` | ✅ Реальный |
| • Конверсия | `orders, clicks` | `(orders / clicks) * 100` | ❌ 0 (clicks=0) |
| • CTR | VK Statistics | `(clicks / shows) * 100` | ❌ 0 |
| • CPC | VK Statistics | `adSpend / clicks` | ❌ 0 |
| **Тренд выручки (таблица)** | `generateChartData()` | Mock данные | ❌ Фейк |
| **Топ 5 товаров** | `getTopProducts()` | Агрегация по заказам | ✅ Реальные |
| **Топ регионов** | `ordersData` | Агрегация по регионам | ✅ Реальные |
| **Выручка по категориям** | `topProducts` | Share % | ✅ Реальные |

#### Проблемы:

🔴 **VK метрики (ad_spend, clicks, shows) = 0**
   - Причина: VK Statistics endpoint не используется в getDashboardSummary
   - Влияние: ROAS, CTR, CPC, Конверсия показывают 0

🔴 **Тренд выручки (таблица) = Mock**
   - Причина: `generateChartData()` использует `Math.random()`
   - Влияние: Графики не соответствуют реальным данным

🟢 **Yandex метрики работают**
   - Выручка, заказы, товары, регионы - все реальные

---

### 2. MARKETING (Marketing.tsx)

#### Источники данных:

```typescript
useEffect(() => {
  const data = await dataService.getCampaigns(period);
  setCampaigns(data);
}, [period]);
```

#### Что отображается:

| Секция UI | Источник данных | Расчеты | Статус |
|-----------|----------------|---------|--------|
| **Таблица кампаний** | `getCampaigns()` | | |
| • Название | `campaigns[].name` | - | ✅ Реальные |
| • Статус | `campaigns[].status` | - | ❌ Заглушка "active" |
| • Показы | `campaigns[].statistics.shows` | - | ❌ 0 |
| • Клики | `campaigns[].statistics.clicks` | - | ❌ 0 |
| • CTR | `campaigns[].statistics.ctr` | - | ❌ 0 |
| • Расход | `campaigns[].spent` | - | ❌ 0 |
| • CPC | `campaigns[].statistics.cpc` | - | ❌ 0 |
| • Выручка | `campaigns[].statistics.revenue` | - | ❌ 0 |
| • ROAS | `campaigns[].statistics.roas` | - | ❌ 0 |
| **Обзор бюджета** | `campaigns` | `(spent / budget_limit) * 100` | ❌ 0/0 |
| **Рейтинг** | `campaigns` | Сортировка по ROAS | ❌ Все 0 |

#### Проблемы:

🔴 **Все статистики = 0**
   - Причина: `dataService.getCampaigns()` НЕ запрашивает VK Statistics
   - Код в `dataService.ts` возвращает заглушки:
     ```typescript
     statistics: {
       shows: 0, clicks: 0, ctr: 0, cpc: 0,
       cpm: 0, conversions: 0, revenue: 0, roas: 0
     }
     ```

🔴 **Статус кампаний = "active" (хардкод)**
   - VK API возвращает `status: null`
   - В коде проставляется `status: "active"`

🟢 **Названия кампаний - реальные**
   - Загружаются из VK API `/campaigns.json`

---

### 3. SALES (Sales.tsx)

#### Источники данных:

```typescript
useEffect(() => {
  const [ordersData, productsData] = await Promise.all([
    dataService.getOrders(period),
    dataService.getTopProducts(period),
  ]);
  setOrders(ordersData);
  setTopProducts(productsData);
}, [period]);
```

#### Что отображается:

| Секция UI | Источник данных | Расчеты | Статус |
|-----------|----------------|---------|--------|
| **Summary Stats (4 карты)** | `orders` | | |
| • Всего заказов | `orders.length` | `count(orders)` | ✅ Реальные |
| • Общая выручка | `orders[].total` | `sum(orders.total)` | ✅ Реальные |
| • Средний чек | `orders` | `totalRevenue / totalOrders` | ✅ Реальный |
| • % доставленных | `orders` | `(DELIVERED / total) * 100` | ✅ Реальный |
| **Таблица заказов** | `orders` | | |
| • ID заказа | `orders[].id` | - | ✅ Реальные |
| • Дата | `orders[].creation_date` | - | ✅ Реальные |
| • Регион | `orders[].region` | - | ✅ Реальные |
| • Товаров | `orders[].items_count` | - | ✅ Реальные |
| • Сумма | `orders[].items_total` | - | ✅ Реальные |
| • Доставка | `orders[].delivery_cost` | - | ✅ Реальные |
| • Итого | `orders[].total` | - | ✅ Реальные |
| • Статус | `orders[].status` | - | ✅ Реальные |
| **Анализ категорий** | Хардкод | - | ❌ Mock |
| **Топ товаров (график)** | `topProducts` | - | ✅ Реальные |

#### Проблемы:

🔴 **Анализ категорий = Хардкод**
   ```typescript
   const categoryBreakdown = [
     { name: "Кухонные гарнитуры", value: 850000, count: 125 },
     // ...
   ];
   ```
   - Не связано с реальными данными
   - Всегда одни и те же цифры

🟢 **Заказы и топ товары - реальные**

---

### 4. PRODUCTS (Products.tsx)

#### Источники данных:

```typescript
useEffect(() => {
  const data = await dataService.getProducts();
  setProducts(data);
}, []);
```

#### Что отображается:

| Секция UI | Источник данных | Расчеты | Статус |
|-----------|----------------|---------|--------|
| **Таблица товаров** | `getProducts()` | | |
| • Название | `products[].name` | - | ⚠️ offerId (нет имен) |
| • Категория | `products[].category` | - | ❌ Пусто |
| • Артикул | `products[].offer_id` | - | ✅ Реальные |
| • Цена | `products[].price` | - | ❌ 0 |
| • Остаток | `products[].stock_total` | - | ✅ Реальные |
| • Статус | `products[].stock_status` | - | ✅ Реальный |
| • Связь с 1C | `products[].has_c1_mapping` | - | ❌ false |
| **Stock Overview** | `products` | Фильтрация | ✅ Реальные |

#### Проблемы:

🔴 **Цены = 0**
   - Yandex API `/offers/stocks` НЕ возвращает цены
   - Нужен другой endpoint: `/offer-prices`

🔴 **Категории = Пусто**
   - Yandex API `/offers/stocks` не возвращает категории
   - Нужен маппинг или другой endpoint

🔴 **Названия = offerId**
   - Yandex API возвращает только offerId
   - Нужен endpoint с полными данными товара

🟢 **Остатки работают**
   - stock_total корректный

---

### 5. REPORTS (Reports.tsx)

#### Статус: ❌ **Полностью нефункциональная**

Все данные - хардкод:
- Шаблоны отчетов: статичный массив
- Последние отчеты: mock данные
- Генерация отчетов: не реализована

---

## 📈 ДЕТАЛЬНЫЙ АНАЛИЗ РАСЧЕТОВ

### dataService.getDashboardSummary()

```typescript
async getDashboardSummary(period: string): Promise<DashboardSummary> {
  // 1. Запрашиваем данные
  const [orders, campaigns] = await Promise.all([
    yandexAPI.getOrders(periodParams.from, periodParams.to),
    vkAPI.getCampaigns(),
  ]);

  // 2. Рассчитываем выручку
  const totalRevenue = orders.reduce((sum, order) => {
    const orderTotal = (order as any).buyerTotal || order.total || 0;
    return sum + orderTotal;
  }, 0);

  // 3. Считаем заказы
  const totalOrders = orders.length;

  // 4. VK метрики (проблема!)
  const adSpend = 0; // ❌ VK Statistics endpoint недоступен
  const totalClicks = 0; // ❌
  const totalShows = 0; // ❌

  // 5. Рассчитываем производные метрики
  const aov = totalOrders > 0 ? totalRevenue / totalOrders : 0;
  const ctr = totalShows > 0 ? (totalClicks / totalShows) * 100 : 0;
  const cpc = totalClicks > 0 ? adSpend / totalClicks : 0;
  const conversionRate = totalClicks > 0 ? (totalOrders / totalClicks) * 100 : 0;
  const roas = adSpend > 0 ? totalRevenue / adSpend : 0;

  return {
    metrics: {
      total_revenue: totalRevenue,  // ✅ Реальный
      total_orders: totalOrders,    // ✅ Реальный
      ad_spend: adSpend,            // ❌ 0
      roas: roas,                   // ❌ 0
      aov: aov,                     // ✅ Реальный
      conversion_rate: conversionRate, // ❌ 0
      ctr: ctr,                     // ❌ 0
      cpc: cpc,                     // ❌ 0
    }
  };
}
```

**Проблема:**
- VK Statistics не запрашивается
- `adSpend`, `clicks`, `shows` = 0
- Все зависимые метрики (ROAS, CTR, CPC, Конверсия) = 0 или NaN

**Решение:**
Нужно добавить запрос VK Statistics:
```typescript
const vkStats = await vkAPI.getStatistics(periodParams.from, periodParams.to);
const adSpend = vkStats.total?.spent || 0;
const totalClicks = vkStats.total?.clicks || 0;
const totalShows = vkStats.total?.shows || 0;
```

---

### dataService.getCampaigns()

```typescript
async getCampaigns(period: string = "30d"): Promise<Campaign[]> {
  const campaigns = await vkAPI.getCampaigns();

  // ❌ Проблема: статистика не запрашивается
  return campaigns.map((campaign: any) => ({
    id: campaign.id,
    name: campaign.name,
    status: "active", // ❌ Хардкод
    spent: 0,         // ❌ 0
    statistics: {
      shows: 0,       // ❌ 0
      clicks: 0,      // ❌ 0
      ctr: 0,         // ❌ 0
      // ... все нули
    },
  }));
}
```

**Проблема:**
- Загружаются только names кампаний
- Статистика НЕ запрашивается
- Все метрики = 0

**Решение:**
Нужно добавить:
```typescript
const periodParams = getPeriodParams(period);
const [campaigns, allAdGroups, stats] = await Promise.all([
  vkAPI.getCampaigns(),
  vkFetch('/ad_groups.json?limit=200'),
  vkAPI.getStatistics(periodParams.from, periodParams.to),
]);

// Сопоставить статистику с кампаниями через ad_groups
```

---

### dataService.getTopProducts()

```typescript
async getTopProducts(period: string): Promise<TopProduct[]> {
  const [products, orders] = await Promise.all([
    yandexAPI.getProducts(),
    yandexAPI.getOrders(periodParams.from, periodParams.to),
  ]);

  const productStats: Record<string, {
    name: string;
    category: string;
    orders: number;
    quantity: number;
    revenue: number;
  }> = {};

  // Агрегируем по offerId
  orders.forEach((order: any) => {
    order.items.forEach((item: any) => {
      const offerId = item.offerId || item.shopSku;
      
      if (!productStats[offerId]) {
        productStats[offerId] = {
          name: item.offerName || offerId, // ✅ Из заказа
          category: product?.category || "Без категории", // ⚠️ Если есть
          orders: 0,
          quantity: 0,
          revenue: 0,
        };
      }
      
      productStats[offerId].orders += 1;
      productStats[offerId].quantity += item.count || 1;
      productStats[offerId].revenue += (item.buyerPrice || item.price || 0) * (item.count || 1);
    });
  });

  // Сортировка по revenue
  return Object.entries(productStats)
    .map(([offerId, stats]) => ({ ...stats, offer_id: offerId }))
    .sort((a, b) => b.revenue - a.revenue)
    .slice(0, 10);
}
```

**Статус:** ✅ Работает корректно
- Название товара берется из `item.offerName` в заказе
- Revenue рассчитывается из `item.buyerPrice * count`
- Категории могут быть пустыми

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. VK STATISTICS НЕ ИСПОЛЬЗУЮТСЯ

**Затронутые секции:**
- Dashboard: KPI Cards (ROAS, CTR, CPC, Конверсия)
- Marketing: Вся таблица кампаний
- Marketing: Обзор бюджета, Рейтинг

**Причина:**
`dataService.ts` НЕ вызывает VK Statistics API

**Решение:**
Добавить в `dataService.getDashboardSummary()` и `dataService.getCampaigns()`:
```typescript
const stats = await vkAPI.getStatistics(dateFrom, dateTo);
```

---

### 2. MOCK ДАННЫЕ В КРИТИЧЕСКИХ МЕСТАХ

**Затронутые секции:**
- Dashboard: Тренд выручки (таблица)
- Sales: Анализ категорий
- Reports: Вся страница

**Причина:**
Хардкод в компонентах

**Решение:**
- Тренд выручки: Группировать реальные заказы по датам
- Анализ категорий: Агрегировать из реальных заказов

---

### 3. НЕПОЛНЫЕ ДАННЫЕ YANDEX

**Затронутые секции:**
- Products: Цены = 0, Категории пусты

**Причина:**
Используется только `/offers/stocks` endpoint

**Решение:**
Добавить запрос `/offer-prices` для цен

---

## ✅ ЧТО РАБОТАЕТ ХОРОШО

1. **Yandex заказы** ✅
   - Корректная пагинация
   - Агрегация из 6 кампаний
   - Все поля заполнены

2. **Top Products расчет** ✅
   - Агрегация revenue из заказов
   - Правильная сортировка

3. **Регионы** ✅
   - Динамический расчет из заказов
   - Share % корректный

4. **VK Token Manager** ✅
   - Автообновление работает
   - Scope сохраняются

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ

### Высокий приоритет:

1. ✅ Добавить VK Statistics в Dashboard
2. ✅ Добавить VK Statistics в Marketing
3. ⚠️ Заменить mock тренд выручки на реальный

### Средний приоритет:

4. ⚠️ Добавить цены в Products (endpoint `/offer-prices`)
5. ⚠️ Заменить анализ категорий в Sales на реальный

### Низкий приоритет:

6. Reports - требует отдельной реализации

---

