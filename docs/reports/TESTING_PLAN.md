# 🧪 План тестирования архитектуры NM Dashboard

**Дата:** 9 декабря 2025  
**Версия архитектуры:** 3.0  
**Цель:** Пошаговое тестирование всех элементов архитектуры через фронтенд

---

## 📋 Методология тестирования

### Принципы:
1. **Последовательность:** От простого к сложному, от базовых компонентов к агрегациям
2. **Зависимости:** Сначала тестируем базовые API, потом компоненты, которые их используют
3. **Группировка:** Связанные элементы тестируются вместе
4. **Логирование:** Все действия и результаты логируются в `logs/`

### Формат тестирования:
- **Шаг:** Номер и название теста
- **Цель:** Что проверяем
- **Действия:** Что делать в UI
- **Ожидаемый результат:** Что должно произойти
- **Проверка логов:** Что смотреть в логах/консоли
- **Зависимости:** От каких тестов зависит

---

## 🔧 ПОДГОТОВКА К ТЕСТИРОВАНИЮ

### Шаг 0.1: Проверка окружения
**Цель:** Убедиться, что приложение готово к тестированию

**Действия:**
1. Проверить наличие `.env` файла с токенами
2. Убедиться, что зависимости установлены (`npm install` или `pnpm install`)
3. Проверить, что папка `logs/` создана

**Ожидаемый результат:**
- ✅ `.env` файл существует
- ✅ `node_modules/` содержит зависимости
- ✅ Папка `logs/` существует

**Проверка логов:**
- Нет критических ошибок при запуске

---

## 📊 ГРУППА 1: БАЗОВЫЕ API ИНТЕГРАЦИИ

### Шаг 1.1: Тестирование Yandex Market API - Кампании
**Цель:** Проверить получение списка кампаний Yandex Market

**Действия:**
1. Открыть страницу **Settings** (`/settings`)
2. Перейти на вкладку **"API токены"**
3. Проверить статус Yandex Market (должен быть "Connected" если токен валиден)
4. Нажать кнопку **"Тест подключения"** для Yandex
5. Проверить результат теста

**Ожидаемый результат:**
- ✅ Статус Yandex показывает "Connected" (зеленый индикатор)
- ✅ Кнопка "Тест подключения" возвращает успех
- ✅ В консоли браузера нет ошибок

**Проверка логов:**
- `logs/api-yandex.log` или консоль сервера: запрос к `/api/yandex/campaigns`
- Кэш: первая загрузка должна быть cache miss, вторая - cache hit

**Зависимости:** Нет

---

### Шаг 1.2: Тестирование Yandex Market API - Заказы
**Цель:** Проверить получение заказов Yandex Market

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Выбрать период **"30 дней"**
3. Дождаться загрузки данных
4. Проверить, что данные загрузились (не белый экран, не ошибка)

**Ожидаемый результат:**
- ✅ Dashboard загружается без ошибок
- ✅ KPI карточки отображаются (даже если значения 0)
- ✅ Нет сообщений об ошибках

**Проверка логов:**
- Консоль браузера: запрос к `/api/dashboard/summary`
- Консоль сервера: запрос к `/api/yandex/orders` с параметрами `date_from`, `date_to`
- Кэш: проверка работы кэширования

**Зависимости:** Шаг 1.1

---

### Шаг 1.3: Тестирование Yandex Market API - Товары
**Цель:** Проверить получение товаров Yandex Market

**Статус:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**

**Действия:**
1. Открыть страницу **Products** (`/products`)
2. Дождаться загрузки данных
3. Проверить отображение товаров (или сообщение "Нет данных")

**Ожидаемый результат:**
- ✅ Страница загружается без ошибок
- ✅ Отображается список товаров или "Нет данных"
- ✅ Нет ошибок в консоли

**Проверка логов:**
- Консоль браузера: запрос к `/api/yandex/products`
- Консоль сервера: запрос к Yandex API `/offers`
- Кэш: проверка TTL (30 минут)

**Зависимости:** Шаг 1.1

**Известные проблемы (требуют доработки):**
- ⚠️ Фильтрация по категориям: все товары имеют "Без категории", фильтр не работает эффективно
- ⚠️ Фильтрация по статусам: работает, но требует улучшения логики определения статусов
- ⚠️ Сортировка: работает только в одну сторону (требует доработки UI для переключения направления)
- ⚠️ Логика отображения: показывается 1,110 товаров в таблице, но карточки считают по всем 38,339 (несоответствие)
- ⚠️ Необходимо добавить фильтрацию/скрытие товаров с нулевым остатком по умолчанию
- ⚠️ Требуется доработка взаимосвязи с другими данными (Yandex, категории)

---

### Шаг 1.4: Тестирование VK Ads API - Токены
**Цель:** Проверить автоматическое обновление VK токенов

**Действия:**
1. Открыть страницу **Settings** (`/settings`)
2. Перейти на вкладку **"API токены"**
3. Проверить статус VK Ads:
   - Должен показывать "Connected" если токен валиден
   - Должен показывать время до истечения токена
4. Нажать кнопку **"Тест подключения"** для VK
5. Проверить результат

**Ожидаемый результат:**
- ✅ Статус VK показывает "Connected" и время до истечения
- ✅ Кнопка "Тест подключения" возвращает успех
- ✅ Если токен истек, он автоматически обновляется

**Проверка логов:**
- Консоль сервера: сообщения о проверке/обновлении токена
- `logs/vk-token.log`: логи обновления токенов (если настроено)

**Зависимости:** Нет

---

### Шаг 1.5: Тестирование VK Ads API - Кампании
**Цель:** Проверить получение кампаний VK Ads

**Статус:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**

**⚠️ ИЗВЕСТНАЯ ПРОБЛЕМА:**
В блоке "Активные кампании VK Ads" отображаются **группы объявлений (ad_groups)**, а не кампании (campaigns). Данные корректные, но терминология неверная. См. `docs/reports/KNOWN_ISSUES.md` для деталей.

**Действия:**
1. Открыть страницу **Marketing** (`/marketing`)
2. Выбрать период **"30 дней"**
3. Дождаться загрузки данных
4. Проверить отображение данных
5. Проверить блок **"Активные кампании VK Ads"** (⚠️ фактически это группы)

**Ожидаемый результат:**
- ✅ Страница загружается без ошибок
- ✅ KPI карточки отображаются (CTR, CPC, CPM, ROAS, Conversion Rate, AOV)
- ✅ Таблица "Impressions/Clicks/Spend Dynamics" отображается
- ⚠️ Блок "Активные кампании VK Ads" показывает группы (требует доработки)

**Проверка логов:**
- Консоль браузера: запрос к `/api/vk/campaigns` и `/api/vk/statistics`
- Консоль сервера: запросы к VK API
- Кэш: проверка работы кэширования

**Зависимости:** Шаг 1.4

---

### Шаг 1.6: Тестирование VK Ads API - Статистика
**Цель:** Проверить получение статистики VK Ads

**Действия:**
1. На странице **Marketing** (`/marketing`)
2. Проверить таблицу "Impressions/Clicks/Spend Dynamics"
3. Убедиться, что данные отображаются по дням
4. Проверить, что значения не отрицательные и логичные

**Ожидаемый результат:**
- ✅ Таблица содержит данные по дням
- ✅ Колонки: Дата, Показы, Клики, Расходы
- ✅ Значения корректные (не NaN, не отрицательные)

**Проверка логов:**
- Консоль сервера: запрос к `/api/vk/statistics` с параметрами `date_from`, `date_to`
- Кэш: проверка TTL (10 минут)

**Зависимости:** Шаг 1.5

---

## 📊 ГРУППА 2: 1C ИНТЕГРАЦИЯ

### Шаг 2.1: Тестирование загрузки файла номенклатуры 1C
**Цель:** Проверить процесс загрузки и парсинга файла 1C

**Действия:**
1. Открыть страницу **Upload1C** (`/upload`)
2. Перейти на вкладку **"Загрузка файла номенклатуры"**
3. Нажать кнопку **"Выбрать файл"**
4. Выбрать файл `data/1c/products_example.xlsx` (или реальный файл номенклатуры)
5. Дождаться автоматического определения листов
6. Выбрать лист (если несколько)
7. Проверить автоматический маппинг колонок
8. При необходимости скорректировать маппинг
9. Нажать кнопку **"Предпросмотр"**
10. Проверить предпросмотр данных
11. Нажать кнопку **"Импортировать"**

**Ожидаемый результат:**
- ✅ Файл загружается без ошибок
- ✅ Листы определяются автоматически
- ✅ Заголовки колонок читаются корректно
- ✅ Маппинг колонок работает
- ✅ Предпросмотр показывает данные
- ✅ Импорт завершается успешно
- ✅ Появляется сообщение об успешном импорте с количеством строк

**Проверка логов:**
- Консоль браузера: нет ошибок при парсинге
- Консоль сервера: запрос к `POST /api/1c/upload-products`
- `logs/1c-upload.log`: логи парсинга и импорта (если настроено)
- Файл `data/1c/products.xlsx` обновлен

**Зависимости:** Нет

---

### Шаг 2.2: Тестирование отображения товаров 1C
**Цель:** Проверить, что загруженные товары отображаются на страницах

**Статус:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**

**Действия:**
1. Открыть страницу **Products** (`/products`)
2. Проверить список товаров
3. Открыть страницу **Stock** (`/stock`)
4. Проверить отображение остатков

**Ожидаемый результат:**
- ✅ На странице Products отображаются товары из загруженного файла
- ✅ Отображаются: название, артикул, цена, остаток
- ✅ На странице Stock отображаются остатки товаров
- ✅ Количество товаров соответствует загруженным

**Проверка логов:**
- Консоль браузера: запросы к `/api/1c/products`
- Консоль сервера: чтение файла `data/1c/products.xlsx`

**Зависимости:** Шаг 2.1

**Известные проблемы (требуют доработки):**
- ⚠️ Страница Products показывает все 38,339 товаров, включая товары с нулевым остатком
- ⚠️ Необходимо добавить фильтрацию по умолчанию (показывать только товары с остатком > 0)
- ⚠️ Логика взаимосвязи с данными Yandex Market требует доработки
- ⚠️ Категории не загружаются из файла (все товары имеют "Без категории")
- ⚠️ Требуется доработка логики отображения для больших объемов данных (пагинация/виртуализация)

---

### Шаг 2.3: Тестирование истории загрузок 1C
**Цель:** Проверить отображение истории загрузок

**Действия:**
1. Открыть страницу **Upload1C** (`/upload`)
2. Перейти на вкладку **"История"**
3. Проверить список загрузок
4. Проверить детали последней загрузки

**Ожидаемый результат:**
- ✅ Отображается список загрузок
- ✅ Последняя загрузка соответствует шагу 2.1
- ✅ Отображаются: имя файла, дата, статус, количество строк
- ✅ Если были ошибки, они отображаются

**Проверка логов:**
- Консоль браузера: запрос к `/api/1c/upload-history`
- Консоль сервера: чтение `data/1c/upload_history.json`

**Зависимости:** Шаг 2.1

---

### Шаг 2.4: Тестирование маппинга товаров 1C ↔ Yandex
**Цель:** Проверить функциональность маппинга товаров

**Действия:**
1. Открыть страницу **Upload1C** (`/upload`)
2. Перейти на вкладку **"Связи товаров"**
3. Проверить список товаров 1C
4. Проверить список товаров Yandex
5. Нажать кнопку **"Автоматический маппинг"**
6. Проверить результаты автоматического маппинга
7. Попробовать создать/обновить маппинг вручную

**Ожидаемый результат:**
- ✅ Отображаются товары из 1C
- ✅ Отображаются товары из Yandex
- ✅ Автоматический маппинг работает (находит совпадения по артикулу/названию)
- ✅ Можно создать маппинг вручную
- ✅ Маппинги сохраняются

**Проверка логов:**
- Консоль браузера: запросы к `/api/mapping/products`, `/api/mapping/auto`
- Консоль сервера: логи маппинга
- Файл `data/1c/product_mappings.json` обновляется

**Зависимости:** Шаг 2.1, Шаг 1.3

---

### Шаг 2.5: Тестирование загрузки отчетов 1C (sales_margin, stock_turnover)
**Цель:** Проверить загрузку отчетов 1C со сложной структурой

**Статус:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**

**⚠️ ИЗВЕСТНАЯ ПРОБЛЕМА:**
Файлы `sales_margin.xlsx`, `stock_turnover.xlsx` и `stock_turnover_agg.xlsx` имеют специфическую структуру - это не простые таблицы, а отчеты с параметрами, заголовками и сложной структурой данных. Текущий парсер не может их обработать.

**Действия:**
1. Попробовать загрузить файл `sales_margin.xlsx`
2. Попробовать загрузить файл `stock_turnover.xlsx`
3. Проверить ошибки парсинга

**Ожидаемый результат:**
- ⚠️ В настоящее время импорт не работает для этих файлов
- ⚠️ Требуется создание специальных парсеров для отчетов

**Требуется доработка:**
- Анализ структуры отчетов (где находятся данные, параметры, группировки)
- Создание специальных парсеров для каждого типа отчета
- Обработка параметров отчета (период, способ расчета)
- Обработка сложной структуры (заголовки, группировки, итоги)

**Зависимости:** Шаг 2.1

**См. также:** `docs/reports/KNOWN_ISSUES.md` - раздел "1C File Upload - Специфические структуры отчетов"

---

## 📊 ГРУППА 3: KPI И РАСЧЕТЫ

### Шаг 3.1: Тестирование Dashboard KPI
**Цель:** Проверить расчет и отображение KPI на Dashboard

**Статус:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ** (частично - все связанное с 1С)

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Выбрать период **"30 дней"**
3. Проверить секцию **"Executive Overview"**:
   - VK Ad Spend
   - ROAS
   - Conversion Rate
   - AOV
4. Проверить секцию **"1С · Базовые показатели"** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**:
   - Выручка 1С
   - Возвраты
   - Чистая выручка
   - Валовая прибыль
   - Кол-во SKU в базе
   - Стоимость остатков
5. Проверить другие секции Dashboard

**Ожидаемый результат:**
- ✅ Все KPI карточки отображаются
- ✅ Значения не NaN, не отрицательные (кроме изменений)
- ✅ ROAS подсвечивается красным если < 3
- ✅ Изменения (Δ%) отображаются корректно и рассчитываются относительно предыдущего периода
- ⚠️ **1С показатели требуют доработки** - из-за недостатка информации часть расчетов может отображать некорректные данные

**Проверка логов:**
- Консоль браузера: запрос к `/api/dashboard/summary`
- Консоль сервера: расчеты KPI, использование функций из `metrics.ts`
- Проверить, что используются функции: `calcRoas`, `calcCtr`, `calcAov`, `calcConversionRate`

**Зависимости:** Шаг 1.2, Шаг 1.5, Шаг 2.1

---

### Шаг 3.2: Тестирование Marketing KPI
**Цель:** Проверить расчет маркетинговых метрик

**Действия:**
1. Открыть страницу **Marketing** (`/marketing`)
2. Выбрать период **"30 дней"**
3. Проверить блок **"Marketing KPI"**:
   - CTR (Click-Through Rate)
   - CPC (Cost Per Click)
   - CPM (Cost Per Mille)
   - Conversion Rate
   - ROAS
   - AOV
4. Проверить, что под каждой метрикой указан источник данных

**Ожидаемый результат:**
- ✅ Все метрики отображаются
- ✅ Значения корректные (CTR в %, CPC/CPM в рублях, ROAS - коэффициент)
- ✅ Указаны источники данных (VK, Yandex, 1C)
- ✅ Нет NaN или отрицательных значений (кроме изменений)

**Проверка логов:**
- Консоль браузера: запрос к `/api/analytics/funnel`
- Консоль сервера: использование функций `calcCtr`, `calcCpc`, `calcCpm`, `calcRoas`

**Зависимости:** Шаг 1.5, Шаг 1.6

---

### Шаг 3.3: Тестирование расчетов по периодам
**Цель:** Проверить корректность расчетов для разных периодов

**Действия:**
1. На странице **Dashboard** (`/`)
2. Последовательно выбрать периоды:
   - **"7 дней"**
   - **"30 дней"**
   - **"90 дней"**
   - **"С начала года"**
3. Для каждого периода проверить:
   - KPI обновляются
   - Значения логичные (не резкие скачки)
   - Период отображается корректно

**Ожидаемый результат:**
- ✅ При смене периода данные обновляются
- ✅ Значения соответствуют выбранному периоду
- ✅ Нет ошибок при переключении
- ✅ Кэш работает (повторный выбор того же периода быстрее)

**Проверка логов:**
- Консоль браузера: запросы с разными параметрами `period`
- Консоль сервера: использование `getPeriodRange` для расчета дат
- Кэш: разные ключи для разных периодов

**Зависимости:** Шаг 3.1

---

## 📊 ГРУППА 4: АГРЕГАЦИЯ И ВОРОНКА

### Шаг 4.1: Тестирование сквозной воронки
**Цель:** Проверить агрегацию данных из всех источников

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Проверить секцию **"Executive Overview"**
3. Проверить, что данные агрегируются из:
   - VK (показы, клики, расходы)
   - Yandex (заказы, выручка)
   - 1C (заказы, выручка, маржа)
4. Проверить расчеты KPI на основе агрегированных данных

**Ожидаемый результат:**
- ✅ Данные из всех источников отображаются
- ✅ KPI рассчитываются на основе агрегированных данных
- ✅ ROAS = (Yandex Revenue + 1C Revenue) / VK Spend
- ✅ Conversion Rate = (Yandex Orders + 1C Orders) / VK Clicks

**Проверка логов:**
- Консоль браузера: запрос к `/api/analytics/funnel`
- Консоль сервера: использование `dataWarehouse.ts` для агрегации
- Проверить нормализацию данных через `yandexNormalizer`, `vkNormalizer`, `oneCDataLoader`

**Зависимости:** Шаг 1.2, Шаг 1.5, Шаг 2.1

---

### Шаг 4.2: Тестирование Data Warehouse
**Цель:** Проверить работу единой точки агрегации данных

**Действия:**
1. Открыть страницу **Marketing** (`/marketing`)
2. Выбрать источник **"All"** (VK + Yandex + 1C)
3. Проверить агрегацию данных
4. Попробовать другие источники: **"VK"**, **"VK + Yandex"**

**Ожидаемый результат:**
- ✅ При выборе "All" данные агрегируются из всех источников
- ✅ При выборе "VK" показываются только VK данные
- ✅ При выборе "VK + Yandex" агрегируются VK и Yandex
- ✅ KPI пересчитываются в зависимости от источника

**Проверка логов:**
- Консоль сервера: использование `getFactsAndDimsForPeriod` из `dataWarehouse.ts`
- Проверить нормализацию в star-schema форматы

**Зависимости:** Шаг 4.1

---

## 📊 ГРУППА 5: КЭШИРОВАНИЕ И ПРОИЗВОДИТЕЛЬНОСТЬ

### Шаг 5.1: Тестирование кэширования Yandex API
**Цель:** Проверить работу кэша для Yandex данных

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Выбрать период **"30 дней"**
3. Дождаться первой загрузки
4. Обновить страницу (F5)
5. Проверить скорость второй загрузки
6. Проверить логи сервера

**Ожидаемый результат:**
- ✅ Первая загрузка делает запрос к Yandex API
- ✅ Вторая загрузка использует кэш (быстрее)
- ✅ В консоли сервера видно "Cache HIT"
- ✅ Данные идентичны

**Проверка логов:**
- Консоль сервера: "Cache HIT" для повторных запросов
- Время ответа: вторая загрузка должна быть быстрее
- Ключи кэша: `yandex:orders:all:${date_from}:${date_to}`

**Зависимости:** Шаг 1.2

---

### Шаг 5.2: Тестирование кэширования VK API
**Цель:** Проверить работу кэша для VK данных

**Действия:**
1. Открыть страницу **Marketing** (`/marketing`)
2. Выбрать период **"30 дней"**
3. Дождаться первой загрузки
4. Обновить страницу (F5)
5. Проверить скорость второй загрузки

**Ожидаемый результат:**
- ✅ Первая загрузка делает запрос к VK API
- ✅ Вторая загрузка использует кэш
- ✅ В консоли сервера видно "Cache HIT"

**Проверка логов:**
- Консоль сервера: "Cache HIT" для VK запросов
- Ключи кэша: `vk:statistics:${date_from}:${date_to}`

**Зависимости:** Шаг 1.5

---

### Шаг 5.3: Тестирование TTL кэша
**Цель:** Проверить автоматическое истечение кэша

**Действия:**
1. Загрузить данные на Dashboard
2. Подождать больше времени, чем TTL (например, 6 минут для orders)
3. Обновить страницу
4. Проверить, что данные обновились

**Ожидаемый результат:**
- ✅ После истечения TTL кэш обновляется
- ✅ Делается новый запрос к API
- ✅ В консоли сервера видно "Cache MISS"

**Проверка логов:**
- Консоль сервера: автоматическая очистка устаревших записей (каждые 5 минут)
- Проверить TTL для разных типов данных

**Зависимости:** Шаг 5.1, Шаг 5.2

---

## 📊 ГРУППА 6: ВАЛИДАЦИЯ И ОБРАБОТКА ОШИБОК

### Шаг 6.1: Тестирование валидации загрузки файлов 1C
**Цель:** Проверить обработку ошибок при загрузке файлов

**Действия:**
1. Открыть страницу **Upload1C** (`/upload`)
2. Попробовать загрузить:
   - Неправильный формат файла (например, .txt)
   - Файл без обязательных колонок (code, name)
   - Файл с некорректными данными (нечисловые значения в числовых полях)
3. Проверить отображение ошибок

**Ожидаемый результат:**
- ✅ Неправильный формат отклоняется с понятным сообщением
- ✅ Отсутствие обязательных полей показывает ошибки
- ✅ Некорректные данные показываются в списке ошибок
- ✅ Ошибки отображаются с указанием строки и поля

**Проверка логов:**
- Консоль браузера: ошибки валидации
- Консоль сервера: логи парсинга с ошибками
- `logs/1c-upload-errors.log`: детальные ошибки (если настроено)

**Зависимости:** Шаг 2.1

---

### Шаг 6.2: Тестирование обработки ошибок API
**Цель:** Проверить graceful handling ошибок API

**Действия:**
1. Временно изменить токен Yandex на невалидный в `.env`
2. Перезапустить сервер
3. Открыть страницу **Dashboard** (`/`)
4. Проверить отображение ошибки
5. Восстановить валидный токен

**Ожидаемый результат:**
- ✅ При невалидном токене показывается понятное сообщение об ошибке
- ✅ Не белый экран, не краш приложения
- ✅ UI показывает состояние ошибки
- ✅ После восстановления токена данные загружаются

**Проверка логов:**
- Консоль сервера: ошибки API с деталями
- `logs/api-errors.log`: логи ошибок API (если настроено)

**Зависимости:** Шаг 1.1

---

### Шаг 6.3: Тестирование Data Quality Report
**Цель:** Проверить отчет о качестве данных

**Действия:**
1. Открыть страницу **Stock** (`/stock`)
2. Нажать кнопку **"Data Quality Report"**
3. Проверить модальное окно с отчетами:
   - Missing articles (отсутствующие артикулы)
   - Duplicate articles (дубликаты артикулов)
   - Missing prices (отсутствующие цены)
   - Zero/negative stock (нулевые/отрицательные остатки)

**Ожидаемый результат:**
- ✅ Модальное окно открывается
- ✅ Отображаются все категории проблем
- ✅ Список проблем соответствует реальным данным
- ✅ Можно закрыть модальное окно

**Проверка логов:**
- Консоль браузера: запрос к `/api/analytics/data-quality`
- Консоль сервера: расчет проблем качества данных

**Зависимости:** Шаг 2.1

---

## 📊 ГРУППА 7: СВЯЗИ И МАППИНГ

### Шаг 7.1: Тестирование связи заказов и рекламы
**Цель:** Проверить логическую связь VK рекламы → Yandex заказы

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Проверить секцию **"Executive Overview"**
3. Проверить расчет ROAS:
   - ROAS = (Yandex Revenue + 1C Revenue) / VK Spend
4. Проверить расчет Conversion Rate:
   - Conversion Rate = (Yandex Orders + 1C Orders) / VK Clicks

**Ожидаемый результат:**
- ✅ ROAS рассчитывается корректно
- ✅ Conversion Rate рассчитывается корректно
- ✅ Значения логичные (ROAS > 0, Conversion Rate в %)

**Проверка логов:**
- Консоль сервера: использование `calcRoas`, `calcConversionRate`
- Проверить агрегацию данных из разных источников

**Зависимости:** Шаг 4.1

---

### Шаг 7.2: Тестирование маппинга товаров
**Цель:** Проверить работу маппинга 1C ↔ Yandex

**Действия:**
1. Открыть страницу **Upload1C** (`/upload`)
2. Перейти на вкладку **"Связи товаров"**
3. Создать несколько маппингов вручную
4. Проверить, что маппинги сохраняются
5. Обновить страницу
6. Проверить, что маппинги остались

**Ожидаемый результат:**
- ✅ Можно создать маппинг вручную
- ✅ Маппинги сохраняются
- ✅ После обновления страницы маппинги остаются
- ✅ Маппинги используются в других частях приложения

**Проверка логов:**
- Консоль браузера: запросы к `/api/mapping/products`
- Файл `data/1c/product_mappings.json` обновляется

**Зависимости:** Шаг 2.4

---

## 📊 ГРУППА 8: UI И UX

### Шаг 8.1: Тестирование навигации
**Цель:** Проверить работу навигации между страницами

**Действия:**
1. Последовательно открыть все страницы через меню:
   - Dashboard (`/`)
   - Marketing (`/marketing`)
   - Sales (`/sales`)
   - Products (`/products`)
   - Stock (`/stock`)
   - Upload1C (`/upload`)
   - Reports (`/reports`)
   - Settings (`/settings`)
2. Проверить, что навигация работает
3. Проверить активное состояние текущей страницы в меню

**Ожидаемый результат:**
- ✅ Все страницы открываются
- ✅ URL меняется корректно
- ✅ Активная страница подсвечивается в меню
- ✅ Нет ошибок при переключении

**Проверка логов:**
- Консоль браузера: нет ошибок роутинга

**Зависимости:** Нет

---

### Шаг 8.2: Тестирование периодов на разных страницах
**Цель:** Проверить работу селекторов периода

**Действия:**
1. На странице **Dashboard** выбрать разные периоды
2. На странице **Marketing** выбрать разные периоды
3. На странице **Sales** выбрать разные периоды
4. Проверить, что данные обновляются

**Ожидаемый результат:**
- ✅ Селекторы периода работают на всех страницах
- ✅ При смене периода данные обновляются
- ✅ Период отображается корректно
- ✅ Используется единый `getPeriodRange` из shared

**Проверка логов:**
- Консоль браузера: запросы с разными параметрами `period`
- Проверить использование `shared/utils/periods.ts`

**Зависимости:** Шаг 3.3

---

### Шаг 8.3: Тестирование состояний загрузки
**Цель:** Проверить отображение состояний загрузки и ошибок

**Действия:**
1. Открыть страницу **Dashboard** (`/`)
2. Проверить состояние загрузки (должен быть Loader)
3. Дождаться загрузки данных
4. Проверить состояние с данными
5. Проверить состояние ошибки (если есть)

**Ожидаемый результат:**
- ✅ При загрузке показывается индикатор загрузки
- ✅ После загрузки данные отображаются
- ✅ При ошибке показывается понятное сообщение
- ✅ Нет белых экранов

**Проверка логов:**
- Консоль браузера: нет ошибок рендеринга

**Зависимости:** Нет

---

## 📊 ГРУППА 9: СИНХРОНИЗАЦИЯ И НАСТРОЙКИ

### Шаг 9.1: Тестирование синхронизации данных
**Цель:** Проверить функциональность синхронизации

**Действия:**
1. Открыть страницу **Settings** (`/settings`)
2. Перейти на вкладку **"Статус синхронизации"**
3. Проверить статусы синхронизации:
   - Yandex Orders
   - Yandex Products
   - VK Statistics
4. Нажать кнопку **"Синхронизировать"** для одного из источников
5. Проверить обновление статуса

**Ожидаемый результат:**
- ✅ Отображаются статусы синхронизации
- ✅ Показывается последняя синхронизация
- ✅ Кнопка "Синхронизировать" работает
- ✅ После синхронизации статус обновляется
- ✅ Кэш очищается при синхронизации

**Проверка логов:**
- Консоль сервера: запрос к `POST /api/settings/trigger-sync`
- Проверка очистки кэша: `cacheManager.deletePattern`

**Зависимости:** Шаг 1.1, Шаг 1.4

---

### Шаг 9.2: Тестирование истории синхронизации
**Цель:** Проверить отображение истории синхронизации

**Действия:**
1. На странице **Settings** (`/settings`)
2. Перейти на вкладку **"История"**
3. Проверить список синхронизаций
4. Проверить детали последней синхронизации

**Ожидаемый результат:**
- ✅ Отображается история синхронизаций
- ✅ Показываются: дата, источник, статус, количество записей
- ✅ История обновляется после новой синхронизации

**Проверка логов:**
- Консоль браузера: запрос к `/api/settings/sync-history`
- Консоль сервера: сохранение истории синхронизации

**Зависимости:** Шаг 9.1

---

## 📊 ГРУППА 10: ПРОИЗВОДИТЕЛЬНОСТЬ И ОПТИМИЗАЦИЯ

### Шаг 10.1: Тестирование производительности загрузки
**Цель:** Проверить скорость загрузки страниц

**Действия:**
1. Открыть DevTools (F12)
2. Перейти на вкладку **Network**
3. Открыть страницу **Dashboard** (`/`)
4. Проверить время загрузки:
   - Время до первого ответа
   - Время полной загрузки
   - Количество запросов
5. Повторить для других страниц

**Ожидаемый результат:**
- ✅ Страницы загружаются за разумное время (< 3 сек)
- ✅ Количество запросов оптимизировано
- ✅ Используется кэширование (повторные загрузки быстрее)

**Проверка логов:**
- Network tab: время запросов
- Консоль сервера: время обработки запросов

**Зависимости:** Все предыдущие шаги

---

### Шаг 10.2: Тестирование работы с большими объемами данных
**Цель:** Проверить производительность с большим количеством товаров/заказов

**Действия:**
1. Загрузить файл номенклатуры с большим количеством товаров (> 1000)
2. Открыть страницу **Products** (`/products`)
3. Проверить скорость загрузки
4. Проверить отображение (пагинация, виртуализация если есть)

**Ожидаемый результат:**
- ✅ Страница загружается за разумное время
- ✅ Данные отображаются корректно
- ✅ Нет зависаний браузера
- ✅ Если есть пагинация, она работает

**Проверка логов:**
- Консоль сервера: время парсинга большого файла
- Консоль браузера: время рендеринга большого списка

**Зависимости:** Шаг 2.1

---

## 📋 ЧЕКЛИСТ ФИНАЛЬНОЙ ПРОВЕРКИ

### После всех тестов проверить:

- [ ] Все страницы открываются без ошибок
- [ ] Все API endpoints отвечают корректно
- [ ] Кэширование работает на всех уровнях
- [ ] Логирование работает и логи пишутся
- [ ] Ошибки обрабатываются gracefully
- [ ] Данные из всех источников агрегируются корректно
- [ ] KPI рассчитываются правильно
- [ ] Периоды работают на всех страницах
- [ ] Загрузка файлов 1C работает полностью
- [ ] Маппинг товаров работает
- [ ] Синхронизация работает
- [ ] Нет утечек памяти (проверить через DevTools)
- [ ] Нет критических ошибок в консоли

---

## 📝 ФОРМАТ ЗАПИСИ РЕЗУЛЬТАТОВ

Для каждого теста записывать:

```
[Шаг X.X] Название теста
✅ / ⚠️ / ❌ Статус
Время выполнения: XX сек
Ошибки: [список ошибок или "нет"]
Замечания: [пользовательские замечания]
```

---

## 🔍 ЧТО ПРОВЕРЯТЬ В ЛОГАХ

### Консоль браузера (DevTools):
- Ошибки JavaScript
- Ошибки сети (404, 500, etc.)
- Время запросов
- Предупреждения React

### Консоль сервера:
- Запросы к API
- Cache HIT/MISS
- Ошибки парсинга
- Обновление токенов
- Логи из `logger.ts`

### Файлы логов (если настроены):
- `logs/api-yandex.log` - запросы к Yandex API
- `logs/api-vk.log` - запросы к VK API
- `logs/1c-upload.log` - загрузки файлов 1C
- `logs/errors.log` - общие ошибки

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ ДЛЯ ПРОВЕРКИ

1. **Дедупликация по ID** - проверить, нет ли дубликатов заказов
2. **Логирование** - проверить, что логи пишутся везде
3. **Агрегация по месяцам** - проверить, работает ли (если реализовано)
4. **Связь заказов** - проверить, работает ли external_order_id

---

**Дата создания:** 9 декабря 2025  
**Версия:** 1.0  
**Статус:** Готов к использованию

