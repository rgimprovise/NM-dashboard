# 🚀 Руководство по развертыванию

## Различия между локальной и VPS версиями

### ✅ Код одинаковый
- Код из одного Git-репозитория
- Все файлы синхронизируются через `git pull`
- Исходники идентичны

### 🔧 Что отличается

#### 1. `.env` файл
- **Локально**: `PORT=3000` (или по умолчанию)
- **На VPS**: `PORT=3003` (или другой свободный порт)
- Токены API могут отличаться

#### 2. Скрипты запуска
- **Локально**: `npm run dev` → запускает Vite (5173) + dev-server (3000)
- **На VPS**: `npm run build` + `npm start` → запускает собранный `node-build.mjs`

#### 3. Файлы запуска
- **Локально**: `server/dev-server.ts` (только API, без статики)
- **На VPS**: `server/node-build.ts` (API + статика из `dist/spa`)

#### 4. Конфигурация
- **Локально**: Vite proxy для `/api` на `localhost:3000`
- **На VPS**: Caddy проксирует `/api` на Express (3003)

---

## 📋 Развертывание на VPS

### Шаг 1: Подготовка
```bash
# На VPS
cd /var/www/nm-dashboard
git pull origin main  # или ваша рабочая ветка
npm install
```

### Шаг 2: Настройка .env
```bash
# Создать .env файл (если еще нет)
cp .env.example .env
nano .env

# Обязательные переменные:
PORT=3003
YANDEX_AUTH_TOKEN=your_token_here
VK_CLIENT_ID=your_client_id
VK_CLIENT_SECRET=your_client_secret
# ... остальные токены
```

### Шаг 3: Сборка
```bash
npm run build
```

### Шаг 4: Запуск через PM2
```bash
# Остановить старый процесс (если есть)
pm2 stop nm-dashboard

# Запустить новый
pm2 start npm --name nm-dashboard -- start

# Или перезапустить
pm2 restart nm-dashboard

# Проверить статус
pm2 status
pm2 logs nm-dashboard
```

### Шаг 5: Настройка Caddy
```caddyfile
nmdashboard.ru {
    # Проксирование API запросов на Node.js сервер
    reverse_proxy /api/* localhost:3003

    # Статические файлы фронтенда
    root * /var/www/nm-dashboard/dist/spa
    file_server

    # SPA fallback (для React Router)
    try_files {path} /index.html
}
```

```bash
# Проверить конфигурацию
sudo caddy validate --config /etc/caddy/Caddyfile

# Перезагрузить Caddy
sudo systemctl reload caddy
```

---

## 🔍 Проверка работоспособности

### 1. Проверить API сервер
```bash
curl http://localhost:3003/api/ping
# Должен вернуть: {"message":"ping"}
```

### 2. Проверить статические файлы
```bash
ls -la /var/www/nm-dashboard/dist/spa
# Должны быть: index.html, assets/, favicon.ico
```

### 3. Проверить логи
```bash
pm2 logs nm-dashboard --lines 50
```

### 4. Проверить в браузере
- Открыть `https://nmdashboard.ru`
- Проверить консоль браузера (F12) на ошибки
- Проверить Network tab на успешные API запросы

---

## 🐛 Решение проблем

### Проблема: "PathError: Missing parameter name"
**Решение**: Уже исправлено в `server/node-build.ts` - используется `app.use()` вместо `app.get("*")`

### Проблема: "Cannot find module './loadEnv.cjs'"
**Решение**: Уже исправлено в `server/index.ts` - добавлен fallback для ручной загрузки .env

### Проблема: Порт занят
**Решение**: Изменить `PORT` в `.env` файле и перезапустить PM2

### Проблема: Статика не отдается
**Решение**: 
1. Проверить, что `npm run build` выполнен успешно
2. Проверить путь в Caddy: `root * /var/www/nm-dashboard/dist/spa`
3. Проверить права доступа: `chmod -R 755 /var/www/nm-dashboard/dist`

---

## 📝 Важные замечания

1. **НЕ нужно разделять ветки** на `local` и `vps` - код одинаковый
2. **Различия только в .env** - файлы в `.gitignore`
3. **Разные точки входа** - `dev-server.ts` (локально) vs `node-build.ts` (VPS)
4. **После `git pull` на VPS** всегда нужно:
   - `npm install` (если изменились зависимости)
   - `npm run build` (пересобрать)
   - `pm2 restart nm-dashboard` (перезапустить)

---

## 🔄 Процесс обновления на VPS

```bash
# 1. Получить обновления
cd /var/www/nm-dashboard
git pull origin main

# 2. Установить зависимости (если нужно)
npm install

# 3. Пересобрать
npm run build

# 4. Перезапустить
pm2 restart nm-dashboard

# 5. Проверить логи
pm2 logs nm-dashboard --lines 20
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверить логи PM2: `pm2 logs nm-dashboard`
2. Проверить логи Caddy: `sudo journalctl -u caddy -n 50`
3. Проверить .env файл на наличие всех необходимых переменных
4. Проверить, что порт не занят: `netstat -tulpn | grep 3003`

