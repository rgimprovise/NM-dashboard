# 📚 Полный справочник Yandex Market Partner API - GET методы

**Только методы для ЧТЕНИЯ данных (без изменений в кабинете)**

Документация: https://yandex.ru/dev/market/partner-api/doc/ru/

---

## 🔑 Авторизация

**Формат заголовка:** `Api-Key: YOUR_TOKEN`

---

## 📊 1. КАБИНЕТЫ И МАГАЗИНЫ

### 1.1 Получение списка кампаний
```
GET /v2/campaigns
```
**Возвращает:** Список всех магазинов (кампаний) с их ID, типами и настройками
**Параметры:** businessId, campaignId

### 1.2 Информация о конкретной кампании
```
GET /v2/campaigns/{campaignId}
```
**Возвращает:** Детальную информацию о магазине/кампании

### 1.3 Настройки кампании
```
GET /v2/campaigns/{campaignId}/settings
```
**Возвращает:** Настройки конкретного магазина

### 1.4 Логин магазина
```
GET /v2/campaigns/{campaignId}/logins
```
**Возвращает:** Логины доступа к магазину

### 1.5 Регион магазина
```
GET /v2/campaigns/{campaignId}/region
```
**Возвращает:** Регион, в котором работает магазин

---

## 📦 2. ТОВАРЫ (OFFERS)

### 2.1 Список всех товаров
```
GET /v2/campaigns/{campaignId}/offers
```
**Возвращает:** Все товары магазина с характеристиками
**Параметры:** page_token, limit (пагинация)

### 2.2 Список товаров (businesses)
```
GET /v2/businesses/{businessId}/offer-mappings
```
**Возвращает:** Сопоставление товаров с карточками Маркета
**Параметры:** page_token, limit

### 2.3 Информация о конкретном товаре
```
GET /v2/campaigns/{campaignId}/offers/{offerId}
```
**Возвращает:** Детальную информацию о товаре

### 2.4 Карточки товаров
```
GET /v2/businesses/{businessId}/offer-cards
```
**Возвращает:** Карточки товаров на Маркете
**Параметры:** page_token, limit

### 2.5 Рекомендации по товарам
```
GET /v2/businesses/{businessId}/offers/recommendations
```
**Возвращает:** Рекомендации по улучшению товаров

### 2.6 Скрытые товары
```
GET /v2/campaigns/{campaignId}/hidden-offers
```
**Возвращает:** Список скрытых товаров и причины скрытия

---

## 📊 3. ОСТАТКИ И ОБОРАЧИВАЕМОСТЬ

### 3.1 Остатки товаров
```
GET /v2/campaigns/{campaignId}/offers/stocks
```
**Возвращает:** Текущие остатки товаров на складах
**Параметры:** page_token, limit, warehouse_id

### 3.2 Оборачиваемость товаров
```
GET /v2/businesses/{businessId}/goods-stats
```
**Возвращает:** Статистика по оборачиваемости товаров

### 3.3 Информация о складских остатках
```
GET /v2/businesses/{businessId}/warehouses/{warehouseId}/stocks
```
**Возвращает:** Остатки на конкретном складе

---

## 💰 4. ЦЕНЫ

### 4.1 Получение цен
```
GET /v2/campaigns/{campaignId}/offer-prices
```
**Возвращает:** Цены на товары
**Параметры:** page_token, limit

### 4.2 Рекомендованные цены
```
GET /v2/businesses/{businessId}/offer-prices/suggestions
```
**Возвращает:** Рекомендации по ценам для повышения продаж

### 4.3 Ценовой карантин
```
GET /v2/campaigns/{campaignId}/price-quarantine
```
**Возвращает:** Товары, находящиеся в ценовом карантине

---

## 🎁 5. АКЦИИ

### 5.1 Список акций
```
GET /v2/businesses/{businessId}/promos
```
**Возвращает:** Все акции бизнеса
**Параметры:** page_token, limit

### 5.2 Информация об акции
```
GET /v2/businesses/{businessId}/promos/{promoId}
```
**Возвращает:** Детали конкретной акции

### 5.3 Товары в акциях
```
GET /v2/businesses/{businessId}/promos/{promoId}/offers
```
**Возвращает:** Список товаров, участвующих в акции

---

## 📋 6. ЗАКАЗЫ

### 6.1 Список заказов
```
GET /v2/campaigns/{campaignId}/orders
```
**Возвращает:** Все заказы магазина
**Параметры:** 
- status (PROCESSING, DELIVERY, DELIVERED, CANCELLED, etc.)
- fromDate, toDate
- page, pageSize

### 6.2 Информация о заказе
```
GET /v2/campaigns/{campaignId}/orders/{orderId}
```
**Возвращает:** Детальную информацию о конкретном заказе

### 6.3 Статусы заказов
```
GET /v2/campaigns/{campaignId}/orders/stats
```
**Возвращает:** Статистику по статусам заказов

### 6.4 Товары в заказе
```
GET /v2/campaigns/{campaignId}/orders/{orderId}/items
```
**Возвращает:** Список товаров в заказе

### 6.5 История изменений заказа
```
GET /v2/campaigns/{campaignId}/orders/{orderId}/history
```
**Возвращает:** История изменений статуса заказа

---

## 🚚 7. ОТГРУЗКИ FBS

### 7.1 Список отгрузок
```
GET /v2/campaigns/{campaignId}/shipments
```
**Возвращает:** Все отгрузки FBS
**Параметры:** status, dateFrom, dateTo

### 7.2 Информация об отгрузке
```
GET /v2/campaigns/{campaignId}/shipments/{shipmentId}
```
**Возвращает:** Детали конкретной отгрузки

### 7.3 Заказы в отгрузке
```
GET /v2/campaigns/{campaignId}/shipments/{shipmentId}/orders
```
**Возвращает:** Заказы, включенные в отгрузку

---

## 🏷️ 8. ЯРЛЫКИ FBS и DBS

### 8.1 Получение ярлыков для заказов
```
GET /v2/campaigns/{campaignId}/orders/{orderId}/labels
```
**Возвращает:** Ярлыки для печати

### 8.2 Ярлыки для отгрузки
```
GET /v2/campaigns/{campaignId}/shipments/{shipmentId}/labels
```
**Возвращает:** Ярлыки для всей отгрузки

---

## 📦 9. FBY-ЗАЯВКИ

### 9.1 Список заявок
```
GET /v2/campaigns/{campaignId}/warehouses/{warehouseId}/stocks
```
**Возвращает:** Заявки на поставку товаров на склад Яндекса

### 9.2 Информация о заявке
```
GET /v2/campaigns/{campaignId}/first-mile/supplies/{supplyId}
```
**Возвращает:** Детали конкретной заявки FBY

---

## 🚗 10. DBS-ДОСТАВКА

### 10.1 Информация о доставке
```
GET /v2/campaigns/{campaignId}/orders/{orderId}/delivery
```
**Возвращает:** Информацию о доставке заказа

### 10.2 Опции доставки
```
GET /v2/campaigns/{campaignId}/delivery/options
```
**Возвращает:** Доступные варианты доставки

---

## 🔄 11. НЕВЫКУПЫ И ВОЗВРАТЫ

### 11.1 Список возвратов
```
GET /v2/campaigns/{campaignId}/returns
```
**Возвращает:** Все возвраты
**Параметры:** status, fromDate, toDate, page, pageSize

### 11.2 Информация о возврате
```
GET /v2/campaigns/{campaignId}/returns/{returnId}
```
**Возвращает:** Детали конкретного возврата

### 11.3 Заказы без выкупа
```
GET /v2/campaigns/{campaignId}/orders?status=UNREDEEMED
```
**Возвращает:** Невыкупленные заказы

---

## 📄 12. ОТЧЕТЫ И ДОКУМЕНТЫ

### 12.1 Отчет по продажам
```
POST /v2/reports/shows-sales/generate
GET /v2/reports/info/{reportId}
GET /v2/reports/file/{reportId}
```
**Возвращает:** Отчет о показах и продажах

### 12.2 Отчет по оборачиваемости
```
POST /v2/reports/goods-turnover/generate
GET /v2/reports/info/{reportId}
```
**Возвращает:** Отчет по оборачиваемости товаров

### 12.3 Отчет по ценам
```
POST /v2/reports/prices/generate
GET /v2/reports/info/{reportId}
```
**Возвращает:** Отчет по ценам на товары

### 12.4 Отчет по остаткам
```
POST /v2/reports/stocks/generate
GET /v2/reports/info/{reportId}
```
**Возвращает:** Отчет по остаткам товаров

### 12.5 Список отчетов
```
GET /v2/reports
```
**Возвращает:** Список всех сгенерированных отчетов

### 12.6 Финансовые документы
```
GET /v2/campaigns/{campaignId}/documents
```
**Возвращает:** Финансовые документы (акты, накладные)
**Параметры:** dateFrom, dateTo, type

---

## ⭐ 13. ОТЗЫВЫ О ТОВАРАХ

### 13.1 Список отзывов
```
GET /v2/businesses/{businessId}/goods-feedback
```
**Возвращает:** Отзывы покупателей о товарах
**Параметры:** page_token, limit

### 13.2 Комментарии к отзывам
```
GET /v2/businesses/{businessId}/goods-feedback/comments
```
**Возвращает:** Комментарии к отзывам

---

## 🚀 14. БУСТ ПРОДАЖ

### 14.1 Рекомендации по бусту
```
GET /v2/businesses/{businessId}/offer-cards/recommendations
```
**Возвращает:** Рекомендации по улучшению товаров для увеличения продаж

### 14.2 Статистика буста
```
GET /v2/campaigns/{campaignId}/stats/boosts
```
**Возвращает:** Статистику по применению буста продаж

---

## 📊 15. ИНДЕКС КАЧЕСТВА

### 15.1 Общий индекс качества
```
GET /v2/campaigns/{campaignId}/quality/index
```
**Возвращает:** Общий индекс качества магазина

### 15.2 Детали индекса качества
```
GET /v2/campaigns/{campaignId}/quality/details
```
**Возвращает:** Детальную информацию о показателях качества

### 15.3 Рекомендации по улучшению
```
GET /v2/campaigns/{campaignId}/quality/recommendations
```
**Возвращает:** Рекомендации по повышению индекса качества

---

## 💬 16. ЧАТЫ

### 16.1 Список чатов
```
GET /v2/businesses/{businessId}/chats
```
**Возвращает:** Все чаты с покупателями
**Параметры:** page_token, limit

### 16.2 История чата
```
GET /v2/businesses/{businessId}/chats/{chatId}/history
```
**Возвращает:** Историю сообщений в чате

### 16.3 Непрочитанные сообщения
```
GET /v2/businesses/{businessId}/chats/unread
```
**Возвращает:** Количество непрочитанных сообщений

---

## 🏢 17. СКЛАДЫ

### 17.1 Список складов
```
GET /v2/campaigns/{campaignId}/warehouses
```
**Возвращает:** Все склады магазина

### 17.2 Информация о складе
```
GET /v2/campaigns/{campaignId}/warehouses/{warehouseId}
```
**Возвращает:** Детали конкретного склада

### 17.3 Склады Маркета
```
GET /v2/warehouses
```
**Возвращает:** Список складов Яндекс.Маркета (справочник)

---

## 📚 18. СПРАВОЧНИКИ

### 18.1 Категории товаров
```
GET /v2/categories
```
**Возвращает:** Дерево категорий Маркета

### 18.2 Информация о категории
```
GET /v2/categories/{categoryId}
```
**Возвращает:** Детали конкретной категории

### 18.3 Регионы
```
GET /v2/regions
```
**Возвращает:** Справочник регионов

### 18.4 Информация о регионе
```
GET /v2/regions/{regionId}
```
**Возвращает:** Детали конкретного региона

### 18.5 Модели товаров
```
GET /v2/models
```
**Возвращает:** Справочник моделей товаров на Маркете

### 18.6 Тарифы Маркета
```
GET /v2/tariffs
```
**Возвращает:** Актуальные тарифы Маркета

### 18.7 Информация об API-ключе
```
GET /v2/auth
```
**Возвращает:** Информацию о текущем API-ключе

---

## 📈 19. СТАТИСТИКА И АНАЛИТИКА

### 19.1 Статистика продаж
```
GET /v2/campaigns/{campaignId}/stats/sales
```
**Возвращает:** Статистику продаж по периодам
**Параметры:** dateFrom, dateTo, groupBy

### 19.2 Статистика показов
```
GET /v2/campaigns/{campaignId}/stats/shows
```
**Возвращает:** Статистику показов товаров

### 19.3 Конверсия
```
GET /v2/campaigns/{campaignId}/stats/conversion
```
**Возвращает:** Показатели конверсии

### 19.4 ABC-анализ
```
GET /v2/businesses/{businessId}/analytics/abc
```
**Возвращает:** ABC-анализ товаров

---

## 🔔 20. УВЕДОМЛЕНИЯ

### 20.1 Настройки уведомлений
```
GET /v2/campaigns/{campaignId}/notifications/settings
```
**Возвращает:** Текущие настройки уведомлений

---

## 📝 ПРИМЕЧАНИЯ

### Пагинация:
Большинство методов возвращающих списки поддерживают пагинацию:
- `page_token` - токен следующей страницы
- `limit` - количество элементов на странице (обычно до 200)

### Фильтрация по датам:
Многие методы поддерживают фильтры:
- `dateFrom`, `dateTo` или `fromDate`, `toDate`
- Формат: `YYYY-MM-DD` или `YYYY-MM-DDTHH:mm:ss+03:00`

### Статусы заказов:
- `PLACING` - оформляется
- `PROCESSING` - в обработке
- `DELIVERY` - в доставке
- `PICKUP` - готов к выдаче
- `DELIVERED` - доставлен
- `CANCELLED` - отменен
- `UNREDEEMED` - не выкуплен

### Типы отчетов:
- `shows-sales` - показы и продажи
- `goods-turnover` - оборачиваемость
- `prices` - цены
- `stocks` - остатки
- `returns` - возвраты
- `shipments` - отгрузки

---

## 🎯 ПРИОРИТЕТНЫЕ МЕТОДЫ ДЛЯ POWER BI

Для построения комплексного дашборда рекомендуем начать с:

1. **Кампании:** `GET /v2/campaigns`
2. **Товары:** `GET /v2/campaigns/{campaignId}/offers`
3. **Заказы:** `GET /v2/campaigns/{campaignId}/orders`
4. **Остатки:** `GET /v2/campaigns/{campaignId}/offers/stocks`
5. **Цены:** `GET /v2/campaigns/{campaignId}/offer-prices`
6. **Отчет по продажам:** `POST /v2/reports/shows-sales/generate`
7. **Отзывы:** `GET /v2/businesses/{businessId}/goods-feedback`
8. **Индекс качества:** `GET /v2/campaigns/{campaignId}/quality/index`
9. **Статистика:** `GET /v2/campaigns/{campaignId}/stats/sales`
10. **Возвраты:** `GET /v2/campaigns/{campaignId}/returns`

---

**Документация:** https://yandex.ru/dev/market/partner-api/doc/ru/
**Версия:** 2.0
**Дата обновления:** Октябрь 2025

