# 📚 Справочник методов VK Ads API v2

**Дата:** 15 октября 2025  
**API Версия:** v2  
**База URL:** https://ads.vk.com/api/v2

---

## 📋 Содержание

1. [Базовые методы](#базовые-методы)
2. [Статистика](#статистика)
3. [Демография и география](#демография-и-география)
4. [Бюджеты](#бюджеты)
5. [Таргетинг](#таргетинг)
6. [Справочники](#справочники)
7. [Примеры использования](#примеры-использования)

---

## ✅ Реализованные методы

### 📁 Рекламные кабинеты (Ad Plans)

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_ad_plans()` | Список всех рекламных кабинетов | `GET /ad_plans.json` |
| `get_ad_plan_by_id(id)` | Информация о конкретном кабинете | `GET /ad_plans/{id}.json` |
| `get_budget_info(id)` | Бюджет и лимиты кабинета | `GET /ad_plans/{id}.json` |

**Пример:**
```python
api = VKAdsAPIv2(access_token, account_id)

# Все кабинеты
plans = api.get_ad_plans()

# Конкретный кабинет
plan = api.get_ad_plan_by_id("8798776")

# Бюджет
budget = api.get_budget_info("8798776")
```

---

### 📊 Кампании (Campaigns)

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_campaigns()` | Список всех кампаний | `GET /campaigns.json` |
| `get_campaign_by_id(id)` | Информация о конкретной кампании | `GET /campaigns/{id}.json` |
| `get_campaign_budget(id)` | Бюджет кампании | `GET /campaigns/{id}.json` |
| `get_campaign_targeting(id)` | Настройки таргетинга кампании | `GET /campaigns/{id}.json` |

**Пример:**
```python
# Все кампании
campaigns = api.get_campaigns()

# Конкретная кампания
campaign = api.get_campaign_by_id("12345")

# Бюджет кампании
budget = api.get_campaign_budget("12345")
# Возвращает: {campaign_id, budget_limit, budget_limit_day, spent, status}
```

---

### 📋 Группы объявлений (Ad Groups)

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_ad_groups(campaign_id)` | Список групп для кампании | `GET /ad_groups.json` |
| `get_ad_group_by_id(id)` | Информация о группе | `GET /ad_groups/{id}.json` |

**Пример:**
```python
# Группы конкретной кампании
groups = api.get_ad_groups(campaign_id="12345")

# Конкретная группа
group = api.get_ad_group_by_id("67890")
```

---

### 🎯 Объявления (Ads)

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_ads(ad_group_id, campaign_id)` | Список объявлений | `GET /ads.json` |
| `get_ad_by_id(id)` | Информация об объявлении | `GET /ads/{id}.json` |
| `get_ad_targeting(id)` | Настройки таргетинга объявления | `GET /ads/{id}.json` |

**Пример:**
```python
# Все объявления кампании
ads = api.get_ads(campaign_id="12345")

# Объявления группы
ads = api.get_ads(ad_group_id="67890")

# Конкретное объявление
ad = api.get_ad_by_id("99999")

# Таргетинг объявления
targeting = api.get_ad_targeting("99999")
```

---

## 📈 Статистика

### Базовая статистика

| Метод | Описание | Dimensions |
|-------|----------|------------|
| `get_statistics()` | Универсальная статистика | Любые |
| `get_statistics_by_campaigns()` | По кампаниям | campaign_id, date |
| `get_statistics_by_ads()` | По объявлениям | banner_id, date |
| `get_statistics_by_groups()` | По группам | ad_group_id, date |

**Доступные метрики:**
- `shows` - показы
- `clicks` - клики
- `spent` - расходы (в копейках)
- `conversions` - конверсии
- `goals` - достижения целей
- `ctr` - CTR (Click-Through Rate)
- `cpc` - CPC (Cost Per Click)
- `cpm` - CPM (Cost Per Mille)
- `uniq_views_count` - уникальные просмотры

**Пример:**
```python
from datetime import datetime, timedelta

# Период
date_to = datetime.now().strftime("%Y-%m-%d")
date_from = (datetime.now() - timedelta(days=30)).strftime("%Y-%m-%d")

# 1. Базовая статистика
stats = api.get_statistics(
    metrics=["shows", "clicks", "spent", "ctr", "cpc"],
    dimensions=["date", "campaign_id"],
    date_from=date_from,
    date_to=date_to
)

# 2. Статистика по кампаниям
stats_campaigns = api.get_statistics_by_campaigns(
    campaign_ids=["12345", "67890"],
    date_from=date_from,
    date_to=date_to
)

# 3. Статистика по объявлениям
stats_ads = api.get_statistics_by_ads(
    ad_ids=["99999", "88888"],
    date_from=date_from,
    date_to=date_to
)

# 4. Статистика по группам
stats_groups = api.get_statistics_by_groups(
    group_ids=["11111", "22222"],
    date_from=date_from,
    date_to=date_to
)
```

---

## 👥 Демография и география

### Демографические данные

| Метод | Описание | Dimensions |
|-------|----------|------------|
| `get_statistics_with_demographics()` | Статистика по полу и возрасту | sex, age, date |

**Доступные значения:**
- **sex**: `M` (мужской), `F` (женский)
- **age**: `12-17`, `18-24`, `25-34`, `35-44`, `45-54`, `55+`

**Пример:**
```python
# Статистика с разбивкой по демографии
stats_demo = api.get_statistics_with_demographics(
    date_from="2025-09-15",
    date_to="2025-10-15",
    campaign_ids=["12345"]
)

# Результат содержит данные по каждому сегменту:
# {sex: "M", age: "25-34", shows: 1000, clicks: 50, ...}
```

### Географические данные

| Метод | Описание | Dimensions |
|-------|----------|------------|
| `get_statistics_with_geo()` | Статистика по регионам | uniq_geo_id, date |

**Пример:**
```python
# Статистика с разбивкой по географии
stats_geo = api.get_statistics_with_geo(
    date_from="2025-09-15",
    date_to="2025-10-15",
    campaign_ids=["12345"]
)

# Результат содержит данные по каждому региону:
# {uniq_geo_id: "1", shows: 5000, clicks: 250, ...}
# где uniq_geo_id - ID региона ВКонтакте
```

---

## 💰 Бюджеты

| Метод | Описание | Возвращает |
|-------|----------|------------|
| `get_budget_info(ad_plan_id)` | Бюджет рекламного кабинета | Лимиты и остаток |
| `get_campaign_budget(campaign_id)` | Бюджет кампании | Лимиты, расходы, статус |

**Пример:**
```python
# Бюджет кабинета
budget_cabinet = api.get_budget_info("8798776")
# Возвращает: полную информацию о кабинете

# Бюджет кампании
budget = api.get_campaign_budget("12345")
# Возвращает:
# {
#   "campaign_id": "12345",
#   "budget_limit": 100000,      # общий лимит (копейки)
#   "budget_limit_day": 5000,    # дневной лимит (копейки)
#   "spent": 45000,              # потрачено (копейки)
#   "status": "active"           # статус
# }
```

---

## 🎯 Таргетинг

| Метод | Описание | Возвращает |
|-------|----------|------------|
| `get_ad_targeting(ad_id)` | Таргетинг объявления | Настройки таргетинга |
| `get_campaign_targeting(campaign_id)` | Таргетинг кампании | Настройки таргетинга |

**Параметры таргетинга:**
- Регионы (geo)
- Пол и возраст (sex, age)
- Интересы (interests)
- Устройства (devices)
- Браузеры (browsers)
- Операционные системы (os)

**Пример:**
```python
# Таргетинг объявления
targeting_ad = api.get_ad_targeting("99999")

# Таргетинг кампании
targeting_campaign = api.get_campaign_targeting("12345")

# Результат может содержать:
# {
#   "geo": [1, 2],           # ID регионов
#   "sex": ["M", "F"],       # пол
#   "age_from": 18,          # возраст от
#   "age_to": 45,            # возраст до
#   "interests": [...],      # интересы
#   "devices": ["mobile"],   # устройства
#   ...
# }
```

---

## 👥 Аудитории и пиксели

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_audiences()` | Список аудиторий | `GET /audiences.json` |
| `get_audience_by_id(id)` | Конкретная аудитория | `GET /audiences/{id}.json` |
| `get_pixels()` | Список пикселей отслеживания | `GET /pixels.json` |

**Пример:**
```python
# Все аудитории
audiences = api.get_audiences()

# Конкретная аудитория
audience = api.get_audience_by_id("12345")

# Все пиксели
pixels = api.get_pixels()
```

---

## 📚 Справочники

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_categories()` | Категории/тематики | `GET /content_categories.json` |
| `get_geo_regions()` | Справочник регионов | `GET /targeting/geo.json` |
| `get_clients()` | Список клиентов (агентство) | `GET /clients.json` |

**Пример:**
```python
# Категории контента
categories = api.get_categories()

# Регионы для таргетинга
regions = api.get_geo_regions()

# Клиенты (для агентских кабинетов)
clients = api.get_clients()
```

---

## 🚀 Комплексная статистика

| Метод | Описание | Включает |
|-------|----------|----------|
| `get_comprehensive_statistics()` | Полная статистика | Кампании, демография, география |
| `get_all_campaigns_with_stats()` | Кампании + статистика | Список кампаний + их метрики |

**Пример:**
```python
# Комплексная статистика
stats = api.get_comprehensive_statistics(
    date_from="2025-09-15",
    date_to="2025-10-15",
    campaign_ids=["12345", "67890"]
)

# Возвращает:
# {
#   "period": {"from": "...", "to": "..."},
#   "stats": {
#     "by_campaigns": {...},      # статистика по кампаниям
#     "demographics": {...},       # демографическая статистика
#     "geography": {...}           # географическая статистика
#   }
# }

# Кампании со статистикой
campaigns_stats = api.get_all_campaigns_with_stats(
    date_from="2025-09-15",
    date_to="2025-10-15"
)

# Возвращает:
# {
#   "campaigns": {...},    # список кампаний
#   "statistics": {...}    # их статистика
# }
```

---

## 👤 Пользователь

| Метод | Описание | Endpoint |
|-------|----------|----------|
| `get_current_user()` | Информация о текущем пользователе | `GET /users/me.json` |

**Пример:**
```python
# Текущий пользователь
user = api.get_current_user()
# Возвращает информацию об авторизованном пользователе
```

---

## 📊 Структура ответов

### Статистика
```json
{
  "rows": [
    {
      "dimensions": {
        "date": "2025-10-01",
        "campaign_id": "12345"
      },
      "metrics": {
        "shows": 10000,
        "clicks": 500,
        "spent": 50000,
        "ctr": 5.0,
        "cpc": 100
      }
    }
  ],
  "totals": {
    "shows": 10000,
    "clicks": 500,
    "spent": 50000
  }
}
```

### Кампании
```json
{
  "items": [
    {
      "id": "12345",
      "name": "Моя кампания",
      "status": "active",
      "budget_limit": 100000,
      "spent": 45000
    }
  ],
  "count": 1
}
```

---

## ⚠️ Важные замечания

### VK Ads API v2 vs Старый API

**Старый API (api.vk.com/method/ads.*):**
- `ads.getAds`
- `ads.getStatistics`
- `ads.getBudget`
- `ads.getDemographics`

**Новый API v2 (ads.vk.com/api/v2):**
- Использует REST endpoints
- Авторизация через Bearer tokens
- Другая структура запросов
- POST для статистики

### Ограничения API

- **Rate Limits:** Рекомендуется не более 3 запросов в секунду
- **Batch Limits:** Фильтры `in` ограничены 50 элементами
- **Date Range:** Максимальный период для статистики - 365 дней

### Коды ошибок

- `401` - Невалидный токен
- `403` - Нет доступа
- `404` - Ресурс не найден
- `429` - Превышен лимит запросов
- `500` - Ошибка сервера

---

## 📖 Полезные ссылки

- **Официальная документация:** https://ads.vk.com/doc/api/info
- **OAuth2 авторизация:** https://ads.vk.com/doc/api/auth
- **Статистика:** https://ads.vk.com/doc/api/statistics

---

## ✅ Итого

**Реализовано методов:** 25+

### Категории:
- ✅ Рекламные кабинеты (3 метода)
- ✅ Кампании (4 метода)
- ✅ Группы объявлений (2 метода)
- ✅ Объявления (3 метода)
- ✅ Статистика (7 методов)
- ✅ Бюджеты (2 метода)
- ✅ Таргетинг (2 метода)
- ✅ Аудитории (2 метода)
- ✅ Справочники (3 метода)
- ✅ Комплексные методы (2 метода)

**Все методы готовы к использованию для полноценной аналитики!** 📊✨

---

**Версия:** 2.1  
**Дата обновления:** 15 октября 2025

