# 🔑 Получение VK Access Token

## Текущее состояние

✅ **Файл `.env` создан и настроен с вашими данными:**
- VK_CLIENT_ID: `b9AHG7669xtg1nvq`
- VK_CLIENT_SECRET: установлен
- YANDEX_OAUTH_TOKEN: установлен

❓ **Нужно получить:**
- VK_ACCESS_TOKEN (access token для API)
- VK_ACCOUNT_ID (ID рекламного кабинета)

---

## 📝 Пошаговая инструкция

### Шаг 1: Получение Access Token

Запустите скрипт:
```bash
python3 get_vk_token.py
```

Скрипт автоматически откроет браузер с OAuth ссылкой VK.

### Шаг 2: Авторизация в VK

1. В открывшемся браузере войдите в свой аккаунт VK
2. Разрешите приложению доступ к VK Ads API
3. После авторизации вас перенаправит на страницу вида:
   ```
   https://oauth.vk.com/blank.html#access_token=vk1.a.ВАШТОКЕН&expires_in=0&user_id=123456
   ```

### Шаг 3: Скопируйте Access Token

Из URL скопируйте значение `access_token`:
- Это часть после `access_token=` и до `&`
- Например: `vk1.a.AbCdEf123456...`

### Шаг 4: Получение Account ID

1. Перейдите в [кабинет VK Рекламы](https://ads.vk.com/)
2. Account ID можно найти:
   - **В URL:** `https://ads.vk.com/hq/dashboard/ad_plan?account_id=ВАШЕ_ЧИСЛО`
   - **В настройках:** Правый верхний угол → Настройки → ID кабинета

### Шаг 5: Обновление `.env` файла

#### Вариант А: Автоматически (через скрипт)
```bash
python3 update_env_tokens.py
```
Скрипт запросит:
1. VK Access Token
2. VK Account ID (опционально)

И автоматически обновит оба файла `.env` и `config.py`.

#### Вариант Б: Вручную
Откройте файл `.env` и замените:
```bash
VK_ACCESS_TOKEN=YOUR_VK_ACCESS_TOKEN
VK_ACCOUNT_ID=YOUR_VK_ACCOUNT_ID
```

На ваши данные:
```bash
VK_ACCESS_TOKEN=vk1.a.ВашТокен
VK_ACCOUNT_ID=12345678
```

---

## 🧪 Проверка настроек

После обновления токенов запустите:

```bash
python3 test_connection.py
```

Скрипт проверит:
- ✅ Подключение к VK Ads API
- ✅ Доступ к рекламному кабинету
- ✅ Подключение к Yandex Market API

---

## 🚀 Запуск сбора данных

После успешной проверки:

```bash
python3 api_data_collector.py
```

---

## 🔧 Быстрая справка

### Созданные файлы:
- ✅ `.env` - основной файл с секретными данными
- ✅ `.env.example` - шаблон для копирования
- ✅ `.gitignore` - защита от случайной публикации секретов

### Доступные скрипты:
- `get_vk_token.py` - показывает OAuth ссылку и инструкции
- `update_env_tokens.py` - обновляет `.env` и `config.py`
- `load_env.py` - утилита для загрузки переменных окружения
- `test_connection.py` - проверка подключения к API

---

## ⚠️ Важно

- **НЕ публикуйте файл `.env` в Git** - он уже добавлен в `.gitignore`
- Access Token имеет неограниченный срок действия (`expires_in=0`)
- Токен дает полный доступ к VK Ads API - храните его в безопасности

---

## 📞 Если что-то не работает

1. **Ошибка "Invalid token":**
   - Проверьте, что скопировали токен полностью
   - Убедитесь, что в токене нет лишних пробелов

2. **Ошибка "Access denied":**
   - При OAuth авторизации убедитесь, что разрешили доступ к VK Ads
   - Scope должен включать `ads`

3. **Не находит Account ID:**
   - Убедитесь, что у вас есть доступ к рекламному кабинету VK
   - Проверьте, что вы авторизованы в правильном аккаунте VK

4. **Пустой ответ от API:**
   - Проверьте, что рекламный кабинет активен
   - Убедитесь, что есть созданные кампании

---

**Удачи! 🎉**

