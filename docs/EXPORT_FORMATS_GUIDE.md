# 📊 Руководство по форматам экспорта данных

**Дата:** 15 октября 2025  
**Версия:** 2.0

---

## 🎯 Поддерживаемые форматы

Все скрипты сбора данных теперь поддерживают три формата экспорта:

1. **JSON** - для Power BI и программной обработки
2. **CSV** - для Excel, Google Sheets и анализа данных
3. **XLSX (Excel)** - полный набор данных на разных листах

---

## 📁 Структура экспорта

### Yandex Market API

**Расположение:** `data/yandex_data/`

#### JSON файлы:
```
✓ campaigns.json              - Список магазинов
✓ campaign_info.json           - Информация о магазине
✓ campaign_settings.json       - Настройки магазина
✓ prices.json                  - Цены на товары
✓ orders_recent_30days.json    - Заказы за последние 30 дней
✓ orders_delivered.json        - Доставленные заказы
✓ orders_processing.json       - Заказы в обработке
✓ orders_cancelled.json        - Отмененные заказы
✓ returns.json                 - Возвраты
```

#### CSV файлы:
```
✓ campaigns.csv                - Магазины (табличный формат)
✓ prices.csv                   - Цены (табличный формат)
✓ orders_recent.csv            - Заказы (табличный формат)
```

#### XLSX файл:
```
✓ yandex_market_data_full.xlsx - Все данные на разных листах
  ├── Campaigns              - Магазины
  ├── Campaign_Info          - Информация о магазине
  ├── Settings               - Настройки
  ├── Prices                 - Цены
  ├── Orders_Recent          - Последние заказы
  ├── Orders_DELIVERED       - Доставленные
  ├── Orders_PROCESSING      - В обработке
  └── Orders_CANCELLED       - Отмененные
```

---

### VK Ads API v2

**Расположение:** `data/vk_data/`

#### JSON файлы:
```
✓ ad_plans.json               - Рекламные кабинеты
✓ ad_plan_current.json        - Текущий кабинет
✓ campaigns.json              - Кампании
✓ ad_groups.json              - Группы объявлений
```

#### CSV файлы:
```
✓ ad_plans.csv                - Кабинеты (табличный формат)
✓ campaigns.csv               - Кампании (табличный формат)
✓ ad_groups.csv               - Группы объявлений (табличный формат)
```

#### XLSX файл:
```
✓ vk_ads_data_full.xlsx       - Все данные на разных листах
  ├── Ad_Plans               - Рекламные кабинеты
  ├── Current_Plan           - Текущий кабинет
  ├── Campaigns              - Кампании
  └── Ad_Groups              - Группы объявлений
```

---

## 🚀 Использование

### Запуск сбора данных

#### Yandex Market (все форматы):
```bash
python3 scripts/collect_yandex_data.py
```

#### VK Ads (все форматы):
```bash
python3 scripts/collect_vk_data_v2.py
```

### Программное управление форматами

```python
from core import YandexMarketAPI, DataExporter

api = YandexMarketAPI(api_key="...", campaign_id="...")

# Только JSON
collect_all_yandex_data(api, export_formats=['json'])

# Только CSV
collect_all_yandex_data(api, export_formats=['csv'])

# Только Excel
collect_all_yandex_data(api, export_formats=['excel'])

# Все форматы (по умолчанию)
collect_all_yandex_data(api, export_formats=['json', 'csv', 'excel'])
```

---

## 📊 Импорт в Power BI

### Вариант 1: JSON (рекомендуется)

**Преимущества:** Полная структура данных, вложенные объекты

**Как импортировать:**
1. **Power BI Desktop** → **Получить данные** → **JSON**
2. Выберите файл: `data/yandex_data/campaigns.json`
3. В **Power Query Editor**:
   - Нажмите на `List` или `Record`
   - Выберите **To Table** (преобразовать в таблицу)
   - Нажмите кнопку развернуть (⇅)
   - Выберите нужные столбцы
   - **Закрыть и применить**

### Вариант 2: CSV (простой)

**Преимущества:** Простой табличный формат, быстрый импорт

**Как импортировать:**
1. **Power BI Desktop** → **Получить данные** → **CSV**
2. Выберите файл: `data/yandex_data/campaigns.csv`
3. Проверьте типы данных
4. **Загрузить**

### Вариант 3: XLSX (все данные сразу)

**Преимущества:** Все таблицы в одном файле

**Как импортировать:**
1. **Power BI Desktop** → **Получить данные** → **Excel**
2. Выберите файл: `data/yandex_data/yandex_market_data_full.xlsx`
3. Выберите нужные листы (таблицы)
4. **Загрузить**

---

## 📈 Импорт в Excel

### CSV файлы

1. Откройте **Excel**
2. **Данные** → **Из текстового/CSV-файла**
3. Выберите нужный `.csv` файл
4. Настройте разделители (запятая)
5. **Загрузить**

### XLSX файл

1. Просто откройте файл `yandex_market_data_full.xlsx` или `vk_ads_data_full.xlsx`
2. Все данные уже организованы на разных листах
3. Готово к анализу!

---

## 🔄 Автоматизация

### Ежедневный сбор данных

**macOS/Linux (crontab):**
```bash
# Редактировать crontab
crontab -e

# Добавить задачи (каждый день в 6:00)
0 6 * * * cd "/Users/rostislavgolivetc/API test" && python3 scripts/collect_yandex_data.py
0 6 * * * cd "/Users/rostislavgolivetc/API test" && python3 scripts/collect_vk_data_v2.py
```

**Windows (Task Scheduler):**
1. Откройте **Планировщик заданий**
2. Создайте задание для каждого скрипта
3. Настройте расписание (ежедневно в 6:00)

---

## 💡 Примеры анализа

### Python + Pandas

```python
import pandas as pd

# Загрузка CSV
campaigns_df = pd.read_csv('data/yandex_data/campaigns.csv')
prices_df = pd.read_csv('data/yandex_data/prices.csv')

# Анализ
print(f"Всего магазинов: {len(campaigns_df)}")
print(f"Средняя цена: {prices_df['price'].mean()}")

# Загрузка Excel (все листы)
excel_file = pd.ExcelFile('data/yandex_data/yandex_market_data_full.xlsx')
for sheet_name in excel_file.sheet_names:
    df = pd.read_excel(excel_file, sheet_name=sheet_name)
    print(f"\n{sheet_name}: {len(df)} записей")
```

### Excel формулы

```excel
=СЧЁТЕСЛИ(Campaigns[status];"ACTIVE")           # Количество активных магазинов
=СРЗНАЧ(Prices[price])                          # Средняя цена
=СУММ(Orders_DELIVERED[total])                  # Общая выручка
```

### Power BI DAX

```DAX
// Количество магазинов
Total Campaigns = COUNT(Campaigns[id])

// Средняя цена
Average Price = AVERAGE(Prices[price])

// Выручка
Total Revenue = SUMX(Orders_DELIVERED, Orders_DELIVERED[total])

// Средний чек
AOV = DIVIDE([Total Revenue], COUNT(Orders_DELIVERED[id]))
```

---

## 🎨 Визуализации в Excel

### Создание сводной таблицы

1. Откройте `yandex_market_data_full.xlsx`
2. Выберите лист **Orders_DELIVERED**
3. **Вставка** → **Сводная таблица**
4. Настройте:
   - **Строки:** Дата
   - **Значения:** Сумма заказов, Количество
   - **Столбцы:** Статус

### Графики

- **Line Chart** - динамика заказов по дням
- **Pie Chart** - распределение по статусам
- **Bar Chart** - топ товаров по продажам

---

## ⚙️ Кастомизация экспорта

### Создание собственного экспортера

```python
from core import DataExporter

exporter = DataExporter()

# JSON с отступом 4
exporter.save_to_json(data, "output.json", indent=4)

# CSV с другой кодировкой
exporter.save_to_csv(data, "output.csv", encoding='cp1251')

# Excel с несколькими листами
all_data = {
    "Sheet1": df1,
    "Sheet2": df2,
    "Sheet3": df3
}
exporter.save_to_excel(all_data, "output.xlsx")
```

### Только нужные форматы

Отредактируйте скрипт:

```python
# Только CSV
collect_all_yandex_data(api, export_formats=['csv'])

# Только Excel
collect_all_vk_data(api, export_formats=['excel'])
```

---

## 📊 Размеры файлов

### Yandex Market

| Формат | Размер | Использование |
|--------|--------|---------------|
| JSON | ~40 KB | Power BI, API |
| CSV | ~10 KB | Excel, анализ |
| XLSX | ~8 KB | Excel, просмотр |

### VK Ads

| Формат | Размер | Использование |
|--------|--------|---------------|
| JSON | ~30 KB | Power BI, API |
| CSV | ~12 KB | Excel, анализ |
| XLSX | ~11 KB | Excel, просмотр |

---

## 🔍 Troubleshooting

### Ошибка при экспорте в CSV

**Проблема:** UnicodeEncodeError

**Решение:**
```python
exporter.save_to_csv(data, "output.csv", encoding='utf-8-sig')
```

### Ошибка при открытии CSV в Excel

**Проблема:** Неправильная кодировка (кириллица)

**Решение:**
1. Откройте через **Данные → Из текстового/CSV**
2. Выберите кодировку **UTF-8**
3. Или сохраните CSV с `encoding='cp1251'`

### Пустой Excel файл

**Проблема:** `all_data` словарь пустой

**Причина:** API не вернул данных

**Решение:** Проверьте логи на ошибки API

---

## 📚 Дополнительные ресурсы

- **Power BI CSV Import:** https://learn.microsoft.com/power-bi/connect-data/desktop-connect-csv
- **Power BI JSON Import:** https://learn.microsoft.com/power-bi/connect-data/desktop-connect-to-data
- **Pandas Documentation:** https://pandas.pydata.org/docs/
- **Excel Power Query:** https://support.microsoft.com/excel

---

## ✅ Итого

- ✅ Три формата экспорта: JSON, CSV, XLSX
- ✅ Автоматическое создание всех форматов
- ✅ Готовые данные для Power BI, Excel, Python
- ✅ Один XLSX файл со всеми данными
- ✅ Простой импорт в любую платформу

**Все готово к анализу!** 📊

---

**Версия:** 2.0  
**Дата обновления:** 15 октября 2025

