# API Data Collector для Power BI Dashboard

Комплексный инструмент для сбора данных из VK Ads API и Yandex Market Partner API для интеграции с Power BI Azure Dashboard.

## 📋 Описание

Этот проект автоматизирует процесс сбора данных из двух основных источников:
- **Yandex Market Partner API** - данные о продажах, товарах, заказах, остатках
- **VK Ads API** - данные о рекламных кампаниях, объявлениях, статистике

Все данные экспортируются в JSON формате для дальнейшей загрузки в Power BI.

## 📊 Доступные категории данных

Подробное описание всех доступных категорий данных смотрите в файле [API_DATA_CATEGORIES.md](API_DATA_CATEGORIES.md)

### Yandex Market (11 категорий):
- Кампании и настройки
- Товары и карточки
- Цены
- Остатки
- Заказы
- Статистика
- Отзывы
- Возвраты
- Склады
- Доставка
- Регионы

### VK Ads (11 категорий):
- Рекламные кабинеты
- Бюджет
- Кампании
- Объявления
- Статистика (показы, клики, конверсии)
- Демография аудитории
- Охват постов
- Целевые аудитории
- Look-alike аудитории
- Клиенты (для агентств)
- Музыканты

## 🚀 Установка

### 1. Клонирование/Загрузка проекта

```bash
cd "API test"
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуемые библиотеки:
- `requests` - для HTTP запросов к API
- `pandas` - для обработки данных
- `openpyxl` - для работы с Excel файлами

### 3. Настройка конфигурации

#### Вариант 1: Прямое редактирование скрипта

Откройте файл `api_data_collector.py` и найдите раздел конфигурации:

```python
# YANDEX MARKET
YANDEX_OAUTH_TOKEN = "YOUR_YANDEX_OAUTH_TOKEN"
YANDEX_CAMPAIGN_ID = "YOUR_CAMPAIGN_ID"

# VK ADS
VK_ACCESS_TOKEN = "YOUR_VK_ACCESS_TOKEN"
VK_ACCOUNT_ID = "YOUR_VK_ACCOUNT_ID"
```

Замените значения на свои токены и ID.

#### Вариант 2: Использование файла конфигурации

1. Скопируйте `config_example.py` в `config.py`:
   ```bash
   cp config_example.py config.py
   ```

2. Отредактируйте `config.py` со своими данными

3. Импортируйте конфигурацию в основном скрипте

## 🔑 Получение токенов и ID

### Yandex Market

1. **OAuth токен:**
   - Перейдите в [Яндекс OAuth](https://oauth.yandex.ru/)
   - Зарегистрируйте приложение
   - Получите OAuth токен для API Маркета

2. **Campaign ID:**
   - Войдите в [личный кабинет Яндекс.Маркета](https://partner.market.yandex.ru/)
   - ID кампании находится в URL или в настройках магазина

📚 [Документация Yandex Market API](https://yandex.ru/dev/market/partner-api/doc/ru/)

### VK Ads

1. **Access Token:**
   - Перейдите в [VK Dev](https://dev.vk.com/)
   - Создайте приложение
   - Получите токен с правами `ads`

2. **Account ID:**
   - Войдите в [кабинет VK Рекламы](https://ads.vk.com/)
   - ID кабинета можно найти в URL или в настройках

📚 [Документация VK Ads API](https://ads.vk.com/help/features/help_api)

## 💻 Использование

### Запуск сбора всех данных

```bash
python api_data_collector.py
```

Скрипт автоматически:
1. Соберет все доступные данные из Yandex Market API
2. Соберет все доступные данные из VK Ads API
3. Сохранит данные в папки `yandex_data/` и `vk_data/`

### Результат выполнения

После успешного выполнения будут созданы две папки с JSON файлами:

```
yandex_data/
├── campaigns.json
├── campaign_info.json
├── campaign_settings.json
├── offer_mappings.json
├── offers.json
├── offer_cards.json
├── prices.json
├── stocks.json
├── orders.json
├── order_stats.json
├── feedbacks.json
├── returns.json
├── warehouses.json
├── delivery_services.json
└── regions.json

vk_data/
├── accounts.json
├── account_info.json
├── budget.json
├── campaigns.json
├── ads.json
├── stats_campaigns_daily.json
├── stats_campaigns_overall.json
├── demographics.json
├── target_groups.json
├── clients.json
└── lookalike_requests.json
```

## 📈 Интеграция с Power BI

### Импорт JSON данных в Power BI

1. **Откройте Power BI Desktop**

2. **Получить данные → JSON:**
   - Выберите `Получить данные` → `JSON`
   - Укажите путь к нужному JSON файлу
   - Power BI автоматически распознает структуру

3. **Power Query - трансформация:**
   ```
   - Раскройте вложенные объекты (Expand)
   - Настройте типы данных
   - Создайте связи между таблицами
   ```

4. **Автоматизация обновления:**
   - Сохраните путь к папкам с данными
   - Настройте расписание обновления
   - Используйте Power BI Gateway для облачной публикации

### Рекомендуемая структура модели данных

```
Факты (Fact Tables):
├── fact_orders (заказы)
├── fact_ad_stats (статистика рекламы)
└── fact_expenses (расходы)

Измерения (Dimensions):
├── dim_products (товары)
├── dim_campaigns (кампании)
├── dim_ads (объявления)
├── dim_regions (регионы)
└── dim_date (календарь)
```

### Ключевые меры (DAX):

```DAX
Total Revenue = SUM(fact_orders[order_amount])
Ad Spend = SUM(fact_ad_stats[spend])
ROAS = DIVIDE([Total Revenue], [Ad Spend], 0)
AOV = DIVIDE([Total Revenue], COUNT(fact_orders[order_id]), 0)
Conversion Rate = DIVIDE(COUNT(fact_orders[order_id]), SUM(fact_ad_stats[clicks]), 0)
```

## ⚙️ Дополнительные возможности

### Использование отдельных API классов

```python
from api_data_collector import YandexMarketAPI, VKAdsAPI

# Yandex Market
yandex = YandexMarketAPI(token="...", campaign_id="...")
campaigns = yandex.get_campaigns()
orders = yandex.get_orders(status="DELIVERED")

# VK Ads
vk = VKAdsAPI(token="...", account_id="...")
campaigns = vk.get_campaigns()
stats = vk.get_statistics(
    ids_type="campaign",
    ids=[123, 456],
    period="day",
    date_from="2024-01-01",
    date_to="2024-01-31"
)
```

### Экспорт в различные форматы

```python
from api_data_collector import DataExporter

exporter = DataExporter()

# JSON
exporter.save_to_json(data, "output.json")

# CSV
exporter.save_to_csv(data, "output.csv")

# Excel (несколько листов)
data_dict = {
    "Sheet1": df1,
    "Sheet2": df2
}
exporter.save_to_excel(data_dict, "output.xlsx")
```

## 📅 Автоматизация сбора данных

### Linux/Mac (cron)

```bash
# Редактировать crontab
crontab -e

# Запускать каждый день в 6:00 утра
0 6 * * * cd /path/to/project && python api_data_collector.py
```

### Windows (Task Scheduler)

1. Откройте `Планировщик заданий`
2. Создайте новое задание
3. Укажите путь к Python и скрипту
4. Настройте расписание (ежедневно/еженедельно)

### Azure Functions (облачная автоматизация)

```python
import azure.functions as func
from api_data_collector import collect_all_yandex_data, collect_all_vk_data

def main(mytimer: func.TimerRequest) -> None:
    # Сбор данных
    collect_all_yandex_data(...)
    collect_all_vk_data(...)
    # Загрузка в Azure Blob Storage
    # ...
```

## 🔍 Troubleshooting

### Ошибка: "Invalid token"
- Проверьте срок действия токена
- Убедитесь, что токен имеет необходимые права
- Обновите токен через OAuth

### Ошибка: "Rate limit exceeded"
- API имеет ограничения на количество запросов
- Добавьте задержки между запросами (`time.sleep()`)
- Используйте пагинацию для больших объемов данных

### Ошибка: "Campaign not found"
- Проверьте правильность Campaign ID
- Убедитесь, что у токена есть доступ к этой кампании

### Пустые данные в ответе
- Проверьте, что есть данные за выбранный период
- Убедитесь, что кампания активна
- Проверьте фильтры и параметры запроса

## 📝 Логирование

Для добавления подробного логирования:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('api_collector.log'),
        logging.StreamHandler()
    ]
)
```

## 🔒 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файлы с токенами в Git
- Добавьте `config.py` в `.gitignore`
- Используйте переменные окружения для production
- Ограничьте права токенов минимально необходимыми

```python
import os

YANDEX_TOKEN = os.getenv('YANDEX_TOKEN')
VK_TOKEN = os.getenv('VK_TOKEN')
```

## 📊 Примеры дашбордов

### Основные KPI:
- Выручка и количество заказов
- ROAS и ROI
- Средний чек
- Конверсия
- Динамика по дням/неделям/месяцам

### Yandex Market:
- Топ товаров по продажам
- География заказов
- Рейтинг и отзывы
- Возвраты и их причины
- Остатки и оборачиваемость

### VK Ads:
- Эффективность кампаний
- CTR и CPC по объявлениям
- Демография аудитории
- Тепловая карта по времени показов
- Сравнение креативов

## 🤝 Поддержка

При возникновении вопросов:
1. Проверьте [API_DATA_CATEGORIES.md](API_DATA_CATEGORIES.md)
2. Изучите официальную документацию API
3. Проверьте логи ошибок
4. Убедитесь, что токены актуальны

## 📚 Полезные ссылки

- [Yandex Market Partner API - Документация](https://yandex.ru/dev/market/partner-api/doc/ru/)
- [VK Ads API - Документация](https://ads.vk.com/help/features/help_api)
- [VK API - Методы](https://dev.vk.com/method)
- [Power BI - Работа с JSON](https://docs.microsoft.com/power-bi/connect-data/desktop-json-data-source)
- [Power BI - DAX функции](https://docs.microsoft.com/dax/)

## 📄 Лицензия

Этот проект создан для внутреннего использования и интеграции с Power BI Azure Dashboard.

---

**Версия:** 1.0  
**Последнее обновление:** Октябрь 2025  
**Автор:** API Data Collector для Power BI

